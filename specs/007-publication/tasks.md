# Unit 007 · Publication — tasks

Scaffolded 2026-09-25. **No row here is signed.** Read `plan.md` first: its §1 names where the
ROADMAP is superseded for this unit, and its §5 is the launch these rows build.

**2026-09-25 — rows close here without being signed.** `✓` means the row's criterion was met and the independent read-only audit said so, with the evidence written into the row. It is not a signature: this unit's scope is signed only at [andres]'s Gate B (T-0714), so the sentence above stays true.

**Count with the command, never from prose:**

```
grep -cE '^\| T-07[0-9]{2} ' specs/007-publication/tasks.md
```

Waves are **unit-scoped** (D-062): 1 to 4. Glyphs: `✓` done · `○` open · `⏸` blocked · `👤` needs
[andres]. Repo: `T` the site template (`agora_transparency`) · `H` the theme (`agora_theme`) · `—`
neither repository: a drupal.org action, or a draft that is never committed.

**Budget (D-044 — an instrument, not a gate): 15 known rows · reserve 4 · stated ceiling 19.**
Binding: at most **2** rows resting on neither necessity nor a signature (today **0**); **0** new
contrib dependencies; **0** new invariants; and a floor — T-0704(d), T-0707 and T-0713 are never
dropped. Every change to this count is accounted in the commit that makes it (I-105).

**Accounting, 2026-09-25, wave 1 (I-105): 16 known rows · reserve 3 · stated ceiling 19.** +1 row, **T-0716** — the theme's own launch text, which no row owned; it rests on necessity. Widened with **0** new rows: **T-0702** (twice: `5b22880`, and this commit), **T-0706** (G8) and **T-0710** (the launch-day run of T-0704). Rows resting on neither necessity nor a signature: **0**. New contrib dependencies: **0**. New invariants: **0**. Floor: **T-0704(d) delivered** — the baseline is in T-0704's row. Closed: **2** of 16 (T-0701, T-0704). **Out of row, outside the count:** `ddece66`, `tests/bin/doctor`, finding G7 of the independent audit. Group 6 ended by telling its reader that every `~/agora-*` rig is rebuilt; one of them is [andres]'s live preview, and on 2026-09-24 a rebuild took it down for about 2 min 40 s while he was looking at it. The commit rests on necessity, touches no packaged file and adds no invariant — no gate runs `doctor`. Read on 2026-09-25: exit 0, group 6 printing `~/agora-smoke  CLONE … <- LIVE PREVIEW - never rebuild in place` and the new closing note; pipeline 975876 10/10.

**Accounting, 2026-09-25, T-0717 (I-105): 17 known rows · reserve 2 · stated ceiling 19.** +1 row, **T-0717**, resting on necessity: this unit's own task file was unread by its spell pre-flight from T-0701's row to its end, and every launch row is written there. New invariants: **0** (`spellcheck` is a pre-flight; no runner executes it). New contrib dependencies: **0**.

**Closed, 2026-09-25: 4 of 17** — T-0701, T-0702, T-0704, T-0716.

**Closed, 2026-09-25, after T-0717: 5 of 17** — T-0701, T-0702, T-0704, T-0716, T-0717.

**Accounting, 2026-09-25, [andres]'s answers (I-105): 20 known rows · stated ceiling 19 — crossed by 1, and recorded rather than re-set (D-044).** +3 rows, each resting on a signature: **T-0718** (D-069), **T-0719** (D-072's first half) and **T-0720** ([andres]'s *"everything must be easy for the user to change"*, item 3 of his answers of 2026-09-25, and necessity). Widened with **0** new rows: **T-0705** (D-072's first half), **T-0706** (D-075, finding Y12 of the independent audit) and **T-0710** (D-068 ruling 5). Rows resting on neither necessity nor a signature: **0**. New contrib dependencies: **0**. New invariants: **0**.

**Closed, 2026-09-25, after [andres]'s answers: 6 of 20** — T-0701, T-0702, T-0703, T-0704, T-0716, T-0717.

**Accounting, 2026-09-25 (I-105): 23 known rows (+3: T-0721, T-0722, T-0723) against a ceiling of 19 — crossed by 4, recorded and not re-set.** T-0721 rests on necessity and on [andres]'s *"everything must be easy for the user to change"*; T-0722 on necessity; T-0723 on D-077, signed by [ejecutor] under D-075. Widened with **0** new rows: T-0708, T-0710, T-0719 and T-0720. Rows resting on neither necessity nor a signature: **0**. New contrib: **0**. New invariants: **0** — T-0719's `tests/bin/static-demo` is to be a build script that no gate runner runs.

**Every rig below is new and throwaway, and is guarded three ways before anything installs:**
`update.settings` → `fetch` → `url` pinned in `settings.php` to the value in
`tests/src/Traits/NoUsageReportingTrait.php`; the same pin in `sites/default/settings.testing.php`,
which core copies into every functional-test site; and `updates.drupal.org` and `www.drupal.org`
resolved to `127.0.0.1` in the web container. `DrupalCmsCompatibilityTest` is never run on a rig:
guarded, it fails by construction; unguarded, it reports usage (T-0635's named residual). No rig is
`~/agora-smoke`.

⚠️ **Inside a table cell a pipe is written `\|`, so a command copied from a cell below must lose the
backslash before it is run.** Measured at `6dc1d06`: T-0706's grep over the packaged text finds **0**
hits copied raw and **9** with a plain `|`.

---

## Wave 1 · True today — nothing here waits on unit 006

Four disjoint lanes: R (T-0701), P (T-0702), X (T-0703), V (T-0704). T-0702 lands between unit 006's
remaining packaged edits, never alongside them (`plan.md` §6).

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-0701 ✓ | T | **Put [andres]'s two release rulings on disk**, as amendments to D-050 with no new number: no more large version steps (2026-09-20), and no tag or release of either project until the official launch unless one is strictly necessary (2026-09-24). Each quoted in his words inside `cspell:disable`<!-- cspell:enable --> markers, with a translation (rule 6). His 2026-09-20 words name no end point, so the reading applied since — patch-only until the launch — is recorded beside them as a reading, never as his words | The amendment heading(s) exist in `specs/000-project/DECISIONS.md` (`grep -c` printed); the numbering command in `open-questions.md` prints the **same** number before and after the commit — the amendment consumes none; `bash tests/bin/cited-tasks-exist` exit 0 with its counts; the pushed commit's job list 10/10, `cspell` among them<br>**MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** `505ecc5` only appends: `specs/000-project/DECISIONS.md` +163/−0, `CLAUDE.md` +2/−0 (rule 8). `grep -c '^## D-050 · Amended 2026-09-25' specs/000-project/DECISIONS.md` prints `1`. The numbering command prints **D-074** at `5b22880` and at `505ecc5`: the amendment consumes no number. Both rulings stand in his words inside `cspell:disable`<!-- cspell:enable --> fences, each translated; the end point read into the 2026-09-20 words is §4's, labelled a reading. `cited-tasks-exist` exit 0 — `citations: 222 · distinct cited: 104 · definitions: 289 · distinct defined: 278 · findings: 0` in `agora-invariants` job 12431795, against 221 · 103 in job 12431413: the one new citation is this amendment's. **Job list:** `505ecc5` has no pipeline of its own — it was pushed with `f40cb22` and `ddece66`, GitLab builds only the tip, and both its files are byte-identical at `ddece66` — so pipeline **975876** read them: 10 jobs, 10 `success`, 0 `allow_failure: true`, `cspell` job 12431794 `Files checked: 441, Issues found: 0`. The two-line pointer it adds to rule 10 of `CLAUDE.md` attributes to him three things §4 labels as a reading — tags, both projects, asking him first — and is reworded in the next commit that touches `CLAUDE.md`. | — |
| T-0702 ✓ | T | **Two shipped comments describe a site that no longer exists.** *Widened 2026-09-25 by ★A, signed by [ejecutor] under standing delegation: 0 new rows.* (i) **`recommended.yml:28-30` says "Neither is done yet" of two things, and one of them is done**: `project_browser` is installed on every Ágora site by `drupal_cms_admin_ui`. Correct the shipped sentence; the permalink wiring stays undone, and the file says why in one line. (ii) **`recipe.yml:583-587` says the newest theme release is 1.1.0 and the components are *"in no tag"***: 1.2.0 and 1.2.1 ship all 4 (`git ls-tree -r <tag> -- components/`). Correct it; the four `?` prefixes stay while `^1.1` admits 1.1.0. `recipe.yml:599` says the object count *"stays at 130"*, and `config-inventory` reads 133 today: the row removes the figure rather than refreshing it | (1) **measured, not read from a recipe**: on a rig, `drush pm:list --status=enabled --field=name` lists `project_browser` exactly once, and the SDC count — the `canvas.component.sdc.agora_theme.*` config objects the four `?` actions name — is **4** with the released theme and **0** with `1.1.0`; the rig, the Drupal CMS version, the `drupal_cms_admin_ui` version and both theme versions printed; (2) every edited sentence is verifiable on disk; (3) `bash tests/bin/packaged-claims` exit 0, its counts unchanged; (4) `git archive --worktree-attributes HEAD \| tar -t \| grep -cx recommended.yml` prints `1`; (5) job list 10/10<br>**(i)–(ii) MET AS WRITTEN, 2026-09-25 — the independent read-only audit's verdict, not a signature. The row stays ○: it is widened again below.** `f40cb22` changes comment lines only — `recipe.yml` and `recommended.yml` parse to identical data at `505ecc5` and `f40cb22`. (1) Rig `~/agora-t0704`, template `6dc1d06` (T-0704's evidence): `drupal/cms` 2.1.6 and `drupal_cms_admin_ui` 2.1.6, both from the rig's own `Locking`/`Installing` lines — `core-recipe-unpack` removes the second from the lock, so `composer show --locked` cannot list it; theme 1.2.1 and 1.1.0; `drush pm:list --status=enabled --field=name \| grep -cx project_browser` prints `1` on all three installs; `canvas.component.sdc.agora_theme.*` counts `4`, all enabled, at 1.2.1 and `0` at 1.1.0. (2) Each edited sentence re-read at source: `drupal_cms_admin_ui` 2.1.6's `recipe.yml:23` installs `project_browser`; this package's `recipes:` applies it and no action names `project_browser.admin_settings`; `recommended.yml` parses to nothing; in the theme, `git ls-tree -r <tag> -- components/` lists `0` files at every tag from 1.0.0 to 1.1.0 and `12` at 1.2.0 and 1.2.1 — four components, three files each. (3) `packaged-claims` exit 0, `files opened: 8 · claims extracted: 36 · comparisons: 49 · one-copy checks: 9 · findings: 0`, identical in `agora-invariants` jobs 12431413 (pipeline 975849, before) and 12431795 (975876, after). (4) `git archive --worktree-attributes HEAD \| tar -t \| grep -cx recommended.yml` prints `1`, of 376 entries. (5) Pipeline **975876** on `ddece66`, the tip of the push that carried `f40cb22`, whose two files are byte-identical there: 10 jobs, 10 `success`, 0 `allow_failure: true`.<br>**WIDENED AGAIN 2026-09-25, 0 new rows, signed by [ejecutor] under standing delegation — found while (i)–(ii) were implemented, and ruled at source.** (iii) **`recipe.yml:150` says the theme is required as `^1.0`; `composer.json:7` says `^1.1`**, since `20418c3` (2026-09-06). The figure leaves that line rather than being refreshed: the floor, and why, is stated at the `agora_theme.settings` action. (iv) **`recipe.yml:410-412` says `config.actions` has NO `?` optionality.** False at both cores CI installs — 11.4.6 (`composer` job 12431788) and 11.4.7 (`Drupal CMS` job 12431789): `ConfigActionManager::applyAction()` returns without applying anything when a `?` name's object is absent (lines 133-144, the file byte-identical at both tags; absent at 11.2.0, present at 11.3.0), and `Recipe::validateConfigActions()` skips such a name on purpose. This file ships four such actions (`:644-651`) and `drupal_cms_admin_ui` 2.1.6 ships one (its `recipe.yml:123`). The same clause stands in `tests/src/Functional/ValidationTest.php:98` and is corrected in the same commit; `specs/003-demo-content/tasks.md:335` (T-1313 ✓) is a closed record and is left as it is. (v) **`recipe.yml:239-241` says a recipe importing a config entity its resolved release lacks *fails the install outright*.** Never measured, and contradicted by reading core: `AllowListConfigStorage::listAll()` intersects the list with what the extension ships, silently, at 11.1.0, 11.2.0, 11.3.0, 11.4.0 and 11.4.6. Measured first on a guarded test rig; corrected only if the measurement contradicts it. Criterion for (iii)–(v): (6) `git grep -n -E 'as .\^1\.0.\|NO .\?. optionality\|optionality available\|applies every entry unconditionally'` prints nothing — 3 lines at `ddece66`; (7) `recipe.yml` parses to identical data at `f40cb22` and at the fixing commit; (8) `packaged-claims` counts unchanged, `8 · 36 · 49 · 9 · 0`; (9) (v)'s measurement written here — rig, command, exit code, objects created — whichever way it falls; (10) the pushed job list 10/10, with `phpunit` and `phpunit-pgsql` both still `OK (22 tests, 2643 assertions)`<br>**(iii)–(v) MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** `d01811d` changes comment lines only in `recipe.yml` (41 lines, 0 outside a comment) and a docblock in `tests/src/Functional/ValidationTest.php`. (6) As written, the pattern also matches this row, which quotes it; scoped away from the process layer — `git grep -n -E 'as .\^1\.0.\|NO .\?. optionality\|optionality available\|applies every entry unconditionally' -- ':!specs/'` — it printed exactly the 3 lines the criterion counts at `ddece66` (`recipe.yml:150`, `:410`, `ValidationTest.php:98`) and prints nothing at `d01811d`. (7) `recipe.yml` parses to identical data at `f40cb22` and `d01811d`, and removing one import makes the comparison `False`, so it is not empty. (8) `packaged-claims` exit 0, `8 · 36 · 49 · 9 · 0`, identical before and after. (9) (v) measured on the guarded rig `~/agora-t0704` at core 11.4.7, theme 1.1.0, Drush 13.8.0.0: a recipe importing `block.block.agora_theme_absent_probe`, which the release does not ship, gives `drush recipe` exit 0, `[OK] … applied successfully`, 0 objects, 0 watchdog rows and 0 config changes in core's checkpoint; the control, a placement the release ships, deleted first, goes 0 → 1. What fails outright: an action with no `?` on an absent object (exit 1, `Exists.php` line 39), and an `import:` keyed by an extension the codebase does not contain (exit 1, `ConfigConfigurator.php` line 103). The comment was corrected to the measurement. (10) Pipeline **975911** on `d01811d`: 10 jobs, 10 `success`, 0 `allow_failure: true`; `phpunit` job 12432353 and `phpunit-pgsql` job 12432354 both `OK (22 tests, 2643 assertions)`; `cspell` job 12432351 `Files checked: 441`. | a rig |
| T-0703 ✓ | — | **The theme's project page is false in at least ten statements against the release it describes (1.2.1)**: the axe figures (*6 pages / 89 / 362*, *6 of 6*), read against the newest nightwatch trace on the day; *"55 contrast pairs"* (the trace prints 68); *"25 colour tokens"* (27 parsed); *"16 template overrides"* (19 at 1.2.1); *"5 stylesheets"* (9 CSS files since 1.2.0); *"not one line of JavaScript"* and *"No JavaScript. Zero files."* (1.2.1 ships `js/table-scroll.js`); *"Nine CI jobs"* (ten); *"not yet covered… Opt-in is in progress"* (it is covered, since 2026-09-05); and the SimplyTest.me link to `agora_transparency/1.0.0` — a version that does not exist, on a launcher that does not handle a general project. [ejecutor] drafts the replacement body — **removing figures rather than refreshing them**, keeping only those a trace prints the same day — and [andres] pastes it | After the paste, from the D7 API (`plan.md` §7): each of the **14** strings its command lists — one per statement above, the axe figures as four and the coverage note as two — is found **0** times; every numeral left in the body is listed in the handed draft with its source<br>**MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** [andres] pasted the replacement body: item 2 of his answers of 2026-09-25, quoted verbatim in `specs/000-project/DECISIONS.md`. Read from the D7 API the same day: node 3618791, `changed` 1790322539 (2026-09-25 07:48:59 UTC), body 9,712 bytes; each of the **14** strings found **0** times. The numerals left, each with its source: `1` ×4 (*"1.x branch"*), `2.2` ×2 (*"WCAG 2.2"*), `2.5.8` and `1.4.10` (success-criterion numbers), `11` (*"Drupal ^11"*) and `2.0` (*"GPL-2.0-or-later"*). | — |
| T-0704 ✓ | T | **Measure what the launch will rest on, before it is scheduled.** *Rewritten 2026-09-25 by ★A, signed by [ejecutor] under standing delegation, because replicating the `Drupal CMS` job on a rig either reports usage or fails by construction.* (a) the Drupal CMS release current on the day, against the version the `Drupal CMS` job builds (2.1.4 today; 2.1.6 released 2026-09-23); (b) this template at `HEAD` with the theme's `1.x` tip read on the day (`7aeb482` today), fetched from drupalcode at that named commit; (c) this template with the lowest theme release `^1.1` admits, `1.1.0`; (d) **the post-release checks of T-0713, run now, where they must fail** | (a) both versions printed — from the job trace and from `updates.drupal.org/release-history/cms/current`; on a rig at the current release, the 12 key packages' locked versions printed beside the CI job's `Locking` lines, and every difference named; `drush site:install` of the template exit 0; the template's own suite printing `OK (22 tests, <m> assertions)`, broken down per class; `DrupalCmsCompatibilityTest` **not run**, and the evidence says so — never a silent skip; (b) and (c) on a rig: the `Locking drupal/agora_theme (<v>)` line, `drush site:install` exit 0, and the suite's totals **equal to CI's at the same template commit** (22 tests / 2643 assertions at `6dc1d06`), or each difference attributed to a class — **MariaDB only, and the evidence says so**; (d) every check T-0713 makes, each recorded with its baseline value: the release feed's root `error`, `packages.drupal.org` **404** ×2, `field_project_composer_types` `[]`, `composer require drupal/agora_transparency` failing under `minimum-stability: stable`, and the install not reached. ⚠️ **(d) failing is the deliverable**<br>**MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** Measured 00:55–01:56 UTC on a new rig, `~/agora-t0704` — PHP 8.3.19, **MariaDB 10.11 only** — guarded three ways before anything installed, with the template at `6dc1d06`; the four commits since (`5b22880`, `505ecc5`, `f40cb22`, `ddece66`) change prose, comments, process files and `tests/bin/doctor`, nothing a clean install or the suite executes. Nothing was written to either working copy; the evidence stays in the session's scratch area. **(a)** `Drupal CMS` job 12430728 (pipeline 975799) prints `_DRUPAL_CMS_TAG 2.1.4`; `release-history/cms/current` lists **2.1.6** first, published. On the rig at 2.1.6, of the 12 key packages only `drupal/cms` differs (2.1.4 against 2.1.6), and the whole lock is 294 `Locking` lines on each side whose one difference is the path package's own version string. `site:install` exit 0; `OK (22 tests, 2643 assertions)`, 0 skipped — InstallTest 1/0, ValidationTest 11/853, AccessibilityTest 1/222, ContentModelTest 6/1253, RequirementsTest 1/177, RolesAndPermissionsTest 2/138. `DrupalCmsCompatibilityTest` **not run**, and the evidence says so. **(b)** Theme `1.x` at `7aeb482`, fetched from `git.drupalcode.org/project/agora_theme.git` at that commit. Composer's lock-file line reads `Upgrading drupal/agora_theme (1.2.1 => 1.x-dev 7aeb482)` — its word for a package already locked, where the criterion says `Locking`. `site:install` exit 0; `OK (22 tests, 2643 assertions)`, the totals CI printed at `6dc1d06` (jobs 12430735 and 12430736). **(c)** Theme 1.1.0: `Locking drupal/agora_theme (1.1.0)`; `site:install` exit 0; `OK (22 tests, 2639 assertions)`. The −4 is one method, `ValidationTest::testCanvasComponentReview`, 120 → 116: none of the four components exists at 1.1.0, so their four `status` assertions have nothing to run on, while the method's denominator — 4 absent, 0 present — runs and passes. **Nothing here asks for a floor above `^1.1`.** **(d)** Baseline at 01:06:30Z, every post-release check failing as it must: the release feed's root is `<error>` (*No release history was found…*); `packages.drupal.org` answers **404** for `agora_transparency.json` and for `agora_transparency~dev.json`; D7 node 3618645 reads `field_project_composer_types = []` and 0 `field_project_images`; a fresh `drupal/cms:2.1.6` reads `minimum-stability: stable`, and `composer require drupal/agora_transparency` **exits 1** (*Could not find package*), leaving no `vendor/`, no lock and no `web/` — the install is not reached. **Usage:** the WebDriver harness's first request lands on the rig site itself, which then runs its own cron (I-120). In (c) its `site_key` request went to the loopback pin and was refused (`cURL error 7`, `127.0.0.1` port 9); in (a) and (b) the same two layers stood but the watchdog was not kept, so there it holds by construction, not by reading. **This is the pre-scheduling measurement. It does not discharge plan §5 precondition 5 — *T-0704 measured on the day* — which T-0710 owns from this commit.** | a rig |
| T-0716 ✓ | H | **The theme's own launch text had no row.** *Added 2026-09-25 from the reserve by [ejecutor] under standing delegation, on the independent audit's finding.* `agora_theme.info.yml:2` and `composer.json:3` say *"In development — see README.md for what exists today."* — the theme's counterpart of the two template strings T-0706 rewrites — and the theme's launch release (T-0708) would carry it. Drop the status clause from both, leaving *"Front-end theme for the Ágora Transparency site template for Drupal CMS."*; no new claim replaces it. Lands on the theme's `1.x` now — no tag and no release (D-050, amended 2026-09-25). Lane P, in the theme's working copy: disjoint from every file the template's lanes touch | In the theme's working copy: `git archive --worktree-attributes HEAD \| tar -xOf - agora_theme.info.yml composer.json \| grep -c 'In development'` prints `0` (`2` at `7aeb482`); `bash tests/bin/identity-strings` exit 0, its check 3 — the two descriptions byte-identical — passing; T-0706's grep re-run over every text file in the theme's `git archive`, each remaining hit listed here with its reason; the pushed job list on `agora_theme` 10/10, none permissive; `git ls-remote drupalcode 'refs/tags/*' \| grep -vcF '^{}'` still `13`<br>**MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** Theme commit `461ae9a` (`task:`) drops the clause from `agora_theme.info.yml:2` and `composer.json:3`; both now read *"Front-end theme for the Ágora Transparency site template for Drupal CMS."*, byte-identical to each other, 73 bytes. In the theme: the archive grep prints `0` (`2` at `7aeb482`); `bash tests/bin/identity-strings` exit 0, its check 3 passing; `gate-a-theme.sh` 44 checks, 0 failures. T-0706's grep over the 97 text files of the theme's `git archive` at `461ae9a` fell from 13 hits to 11. The two that went are the two descriptions, and the 11 left are about pipelines, drift records, test paths and tags with no release — none claims the theme is unreleased. Pipeline **975898** on `461ae9a`: 10 jobs, 10 `success`, 0 `allow_failure: true`. Tags on drupalcode: still `13`. | — |
| T-0717 ✓ | T, then H | **The spell gate reads every line it counts.** *Added 2026-09-25 from the reserve by [ejecutor] under standing delegation, on the independent audit's finding.* cspell honours its block-disable directive wherever the string appears, prose included, and stays off until an enable with the same prefix: eight prose mentions, added on seven days by eight commits, left 642 lines in six template files and one theme file unread while the job counted every one of those files as checked (planted-word control: 0 of 31 flagged with the directives intact, 31 of 31 with them neutralised). The one token they hid is a quoted example, now scoped where it is quoted. Fix every accidental span; add an offline pre-pass to `tests/bin/spellcheck` in both repositories that refuses an unfenced or end-of-file span (rules F1–F5), with exactly one declared exception — the span inside signed D-052/D-053, left under rule 8 and pinned by its opener line's sha256; key the cached dictionaries by core branch. No new invariant: `spellcheck` is a pre-flight that no runner executes | Pre-pass on the template: `findings 0` (6 on the pre-fix tree); on the theme: `findings 0` (1 before); planted-word control: every planted word outside the declared exception flagged, the one inside it not; each of F1–F5 watched failing; parity with cspell-lib's own expressions; `file set: 480` and `Files checked: 439, Issues found: 0` unchanged; wave 1 100·0 and wave 3 67·0 with both self-checks; `claims-match-sources` 14 comparisons, 0 mismatches; theme: `shared-invariants` 7 declared, `--upstream` 0 behind, `gate-a-theme.sh` 44·0; both pushed job lists 10/10<br>**MET 2026-09-25 — the independent read-only audit's verdict, not a signature.** **Template, `fc7b0ae`**: the pre-pass reported 6 findings on the pre-fix tree and 0 after, over 91 block spans (86 fences · 4 self-closing prose pairs · 1 of 1 declared exception) and 2 disable-next directives; its span list is identical, entry for entry, to the one cspell-lib 9.8.0's own expressions produce; planted-word control `Issues found: 11 in 6 files` — every word outside the declared exception flagged, the one inside it not, and the accented token outside its scoped file flagged; F1–F5, a changed version constant and an empty file list each watched failing; `file set: 480` and `Files checked: 439, Issues found: 0` unchanged; wave 1 `100 checks - 0 failures` and wave 3 `67 checks - 0 failures`, both self-checks equal. Pipeline **976077** on `bb6bb43`: 10 jobs, 10 `success`, 0 `allow_failure: true`; `cspell` job 12435144 `Files checked: 441, Issues found: 0`, now over the lines it had counted without reading; `phpunit` job 12435146 and `phpunit-pgsql` job 12435147 both `OK (22 tests, 2643 assertions)`. **Theme, `5710f08`**: the pre-pass reported 1 finding before (`tests/bin/spellcheck:223-228`, F1) and 0 after; planted words 1 of 2 flagged before, 2 of 2 after; `shared-invariants` 7 declared, `--upstream` for `spellcheck` `+0/-0`, `upstream-drift` 0 behind, `gate-a-theme.sh` 44 checks, 0 failures. Pipeline **976131** on `5710f08`: 10 jobs, 10 `success`, 0 `allow_failure: true`; `cspell` job 12435841 `Files checked: 96, Issues found: 0`. The theme's copy carries one adaptation this row did not foresee: a scoped ignore for one word that the template declares in its word list and the theme does not. | — |
| T-0718 ○ 👤 | T, H | **The process disclosure becomes one sentence, per D-069.** *Added 2026-09-25 by [ejecutor] under D-075, on D-069 signed by [andres].* In the packaged set: `README.md`'s "Development process" section is replaced whole by a section headed "How Ágora is built" that holds D-069's sentence; `AGENTS.md`'s audience header keeps its audience statement and loses its pointer to the maintainers' process file; `.gitattributes` follows, in its cross-reference and in its process-layer comment; three incidental mentions of that process file are reworded; and `README.md:522-524`, which says the job logs cannot be re-read from here, is corrected, because they are public. The exact texts are in the section below this table. In the theme, the same sentence closes its own `README.md`. 👤 [andres] pastes the sentence into the template's project page, node 3618645; it is true today, so the paste need not wait for the launch | (1) over every text file of `git archive --worktree-attributes HEAD`: `human-in-the-loop` 0, `co-authorship` 0, `cannot be re-read` 0; the files naming `CLAUDE.md` or `.claude` are exactly `.gitattributes` (2 lines) and `LICENCE-MANIFEST.md` (1 line), each count printed; (2) the sentence is found exactly once in `README.md`; (3) `bash tests/bin/identity-strings` exits 0; (4) `bash tests/bin/packaged-claims` prints the same counts before and after; (5) `recipe.yml` parses to identical data before and after; (6) `bash tests/bin/spellcheck`: pre-pass findings 0, `Issues found: 0`, file set unchanged; (7) the pushed job list 10/10 in both repositories, the theme's half being its `README.md`; (8) 👤 after his paste, the D7 API for node 3618645 counts `human-in-the-loop` 0 and `developed with AI coding assistants` 1, and its `changed` is later than 1790262847 (2026-09-24 15:14:07 UTC) | — |
| T-0719 ○ 👤 | T | **The demo, per D-072; rests on D-072's first half.** *Added 2026-09-25 by [ejecutor] under D-075, on D-072's first half signed by [andres].* There will be a demo of Ágora showing its demonstration content, and the launch does not wait for it. **Its form — a static snapshot (S) or a live Drupal site (L) — and its host are [andres]'s, and open (D-072).** Now, on a new rig guarded three ways, the static form's feasibility is measured, because it prices one of the two forms he chooses between. The real build follows the template's tag, in the form and on the host he rules — under S, between T-0711 and T-0712, from the tag and the released theme. [andres] hosts it and adds the Demo link to the project page; `recipe.yml` carries no Demo link (D-074). Lane V | (1) the static crawl of `HEAD` on `~/agora-demo`: pages crawled, files, bytes, internal links checked (0 broken), forms found = forms replaced, and `/user/*` links replaced, each printed; (2) 0 requests to `updates.drupal.org` or `www.drupal.org`, read from the pin's refusals; (3) the procedure written verbatim beside this row, so every release can rebuild it; (4) the real build, in the form and on the host he rules<br>**WIDENED 2026-09-25, 0 rows, [ejecutor] under D-075:** the static build is a script, `tests/bin/static-demo`, per the independent read-only audit's specification: feasibility on a rig; the real build between T-0711 and T-0712; the form and the host stay [andres]'s (D-072) | D-072's open half and T-0711, for the real build only |
| T-0720 ○ | T | **"Make it yours": the post-install configuration, written down and measured.** *Added 2026-09-25 by [ejecutor] under D-075.* It rests on [andres]'s *"everything must be easy for the user to change"* (item 3 of his answers of 2026-09-25) and on necessity: point 8 of the ROADMAP's unit 006 names post-install configuration, the package documents none, and T-0607 missed it. Steps: site name; logo; masthead (theme settings: upload or path, empty for a plain band — `agora-theme/theme-settings.php:109-117`); currency (the theme's read-only panel links the seven money fields, D-051 §7); social links; completing the accessibility statement and legal pages; the Config Guardian baseline export (its exact form pending a measurement in progress); removing the demonstration records. The rig half runs now; the README section lands in lane P in wave 2, before T-0706 | On a new rig guarded three ways: every step performed as written; entities removed, counted by type before and after — nodes, media, files, terms, menu links, Canvas pages — and what remains named, or 0; the front page answers 200; no PHP warning in `watchdog`; `bash tests/bin/packaged-claims` exit 0; the pushed job list 10/10<br>**WIDENED 2026-09-25, 0 rows, [ejecutor] under D-075:** README steps for Trash, the Canvas page-regions warning (I-112), the privacy stub, the currency panel and D-076's command; the baseline step names Config Guardian's own Sync → Export and is blocked by T-0542; and the criterion adds, after the steps: "1970" 0, "Fuentelclaro" 0, and the files left over named, which must be only the configuration-held ones | — |

### T-0718 · the exact texts, per D-069

Written here once, so the commit that implements T-0718 copies rather than composes. Line numbers
were read at `e003265`; the commit locates each text by its content.

**`README.md:643-665`**, the "Development process" section, is replaced whole by:

```markdown
## How Ágora is built

Ágora is developed with AI coding assistants as part of its tooling, under the maintainer's
direction and held to the project's automated checks; the maintainer is accountable for every
release, and the project's decisions are recorded, with their reasoning, in its
[public repository](https://git.drupalcode.org/project/agora_transparency/-/tree/1.x/specs).
```

The link is absolute on purpose: a relative link to `specs/` dangles in the tarball, and T-0706
requires every link to resolve inside the tarball or be absolute.

**`AGENTS.md:5-8`** is replaced by the lines below. The first mention still reads "Ágora
Transparency" across the wrap, which is `identity-strings` assertion 4; this is the file every Ágora
site receives at its root (`composer.json:27`).

```markdown
> **Audience.** This file is for AI assistants working on a Drupal site **built with the Ágora
> Transparency site template** (`drupal/agora_transparency`) — not on the template itself. Do not
> read the notes below as instructions for changing the template.
```

**`.gitattributes:8`**: the cross-reference `"Development process"` becomes `"How Ágora is built"`.
**`.gitattributes:68-71`** is replaced by the lines below; lines 72-74 stay, because they are the
rule that excludes those files.

```
# --- Process layer (D-015.2) ----------------------------------------------------------
# How this template is built: the project's working rules and its signed decision record.
# Public in the repository, but not part of a release the end user installs.
```

**The incidental mentions of the maintainers' process file, reworded:**

- `README.md:464`: "for the reason `CLAUDE.md` merged its own pair" becomes "for the reason the
  project's own records merged theirs".
- `README.md:553-554`: "stood in `CLAUDE.md` across" becomes "stood in the project's own records
  across".
- `recipe.yml:172`, a comment: "(CLAUDE.md rule 2)" becomes "(rule 2 of the project's SBOM policy)".
- **Kept:** `LICENCE-MANIFEST.md:179` and `.gitattributes:72-73`. They are the inventory of what is
  excluded and the rule that excludes it.

**`README.md:522-524`** becomes:

```markdown
PostgreSQL. Those two lines are quoted in full in D-040(2), and anyone can re-read them without an
account: a job's whole log is public at
`https://git.drupalcode.org/project/agora_transparency/-/jobs/<id>/raw` (follow the redirect),
although the API's `/jobs/<id>/trace` endpoint answers **401** to an anonymous request.
```

**What `packaged-claims` binds there: nothing.** None of the 18 README comparisons listed in its
header reads those lines, and the new section states no registered figure. The only reference to
the old heading is `.gitattributes:8`, fixed above; no anchor link targets `#development-process`;
and `gate-a-wave1.sh` G8 asserts that `AGENTS.md` travels, not what it says. Predicted:
`packaged-claims` counts equal before and after, 0 findings.

**In the theme's working copy:** the same sentence closes its `README.md`, in a section headed
`## How Ágora is built`, placed before `## License`.

**What [andres] pastes into node 3618645**, the template's project page. It replaces the body's last
paragraph, which read on 2026-09-25:

`<p>Development happens in the open, with human-in-the-loop AI assistance. The decisions and their reasoning are recorded under <code class="language-php">specs/</code> in the repository.</p>`

with:

`<p>Ágora is developed with AI coding assistants as part of its tooling, under the maintainer's direction and held to the project's automated checks; the maintainer is accountable for every release, and the project's decisions are recorded, with their reasoning, in its <a href="https://git.drupalcode.org/project/agora_transparency/-/tree/1.x/specs">public repository</a>.</p>`

The sentence belongs on the page and not only in the README: the page is what Drupal.org visitors
and reviewers read first, and a disclosure found only inside the repository would read as hidden.

## Wave 2 · Launch readiness — after unit 006 closes and the rulings exist

T-0705 needs only D-072 and may land earlier; it touches no packaged file. T-0706 is lane P and
waits for unit 006's last packaged edit.

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-0705 ○ | T | **`.tugboat/` per D-072**: deleted, or made safe | **Deleted (D-072 A):** `git ls-files .tugboat \| wc -l` prints `0`, and the packaged text names nothing that no longer exists (count printed); job list 10/10. **Kept (B or C):** `.tugboat/tugboat-settings.txt` pins the fetch URL to the trait's loopback value (count `1`); `grep -cE -- '--stability=dev\|minimum-stability dev' .tugboat/config.yml` prints `0`; `tests/bin/no-usage-reporting` reads `.tugboat/` and was **watched failing** with the pin removed before it passed; job list 10/10<br>**D-072's first half (2026-09-25): a demo, not a Tugboat preview — so this row takes its Deleted branch; its 'Kept (B or C)' put B with C, and B never needed `.tugboat/`.** **Landed in 7149c04.** | D-072 |
| T-0706 ○ | T | **The launch text**, in the packaged set: every sentence that stops being true on launch day, rewritten in the commit that will be tagged — the status heading and paragraph (`README.md:9-14`), the *"not released yet"* installation note (`README.md:129-133`), the support note that says there is no release (`README.md:621-624`), and both `description` strings that say *"In development"* (`composer.json:3`, and `recipe.yml:9`, which the installer shows). The community route's install path written down: `composer require drupal/agora_transparency` into a Drupal CMS project, then the installer or `drush site:install`. `extra.drupal_cms_installer` per D-074. **Not touched:** the process-disclosure section (D-069's) and the lines naming the cited assistant (D-054's) | The inventory **re-derived by command on the day**, not carried from this cell: every text file in `git archive` of `HEAD` searched with `grep -n -i -E 'in development\|not released\|no release\|not finished\|does not resolve'`, each hit edited or left with its reason in this row; every link in a rewritten section resolves **inside the tarball** or is absolute (today `README.md` sends a tarball reader to `.github/workflows/phpunit.yml`, which the tarball omits); `git diff -U0` of the commit contains **0** lines matching `cited assistant\|with citations`; `bash tests/bin/packaged-claims` exit 0 with its counts; job list 10/10<br>**WIDENED 2026-09-25, 0 new rows, signed by [ejecutor] under standing delegation (finding G8 of the independent audit).** `README.md:541-549` states `spellcheck`'s two local figures — files offered, files checked — and nothing binds them: `packaged-claims` reads both only to compare their difference with the 37 + 4 the same section states (`cspell_gap`, `tests/bin/packaged-claims:711-720`). So every new tracked text file anywhere in the repository forces an edit to packaged text. In this row's commit the pair and its dated history leave `README.md`, which keeps the 37 + 4 = 41 accounting that `cspell_split` and `cspell_binaries` still bind; `cspell_gap` goes with its subject, and the commit argues it as following the subject, not weakening a check. `packaged-claims`' extracted-claim and comparison counts fall by exactly what was removed, predicted before the run; every other count unchanged<br>**WIDENED 2026-09-25, 0 new rows, signed by [ejecutor] under D-075 (Y12 of the independent audit):** the launch commit also strips the packaged README's dated self-corrections (17 ⚠️ markers and 10 dated 'until 2026-' lines, counted at `e003265`; `README.md:16-18`, `:74-78`, `:286-297` among them); their history stays in git and `specs/`. | unit 006 closed (T-0627) · D-069 · D-074 · T-0704 |
| T-0707 ○ | — | **The drafts, handed and never committed**: release notes for the theme's launch release and for the template's first release — description, short description, and whether each is new features, bug fixes or both (rule 10) — the replacement body of both project pages, and the template page's image | Handed in the launch report from the session's scratch area; `git status --short` in both working copies shows none of them; every numeral carries the job id whose `/-/jobs/<id>/raw` prints it, read the same day — **0** figures without a source; the process-disclosure wording is whatever D-069 rules, and nothing else | T-0706 · D-069 · D-070 |
| T-0721 ○ | H | **An uploaded logo, and the masthead's opening line, as a site owner changes them.** *Added 2026-09-25 by [ejecutor] under D-075, on the independent read-only audit's plan. Rests on (i) necessity and (ii) [andres]'s *"everything must be easy for the user to change"* (item 3 of his answers of 2026-09-25).* (i) A 240×64 logo uploaded through theme settings renders 44±1 px tall and 165±1 px wide at 1280 and 390, with the slogan aligned and the default mark still 44×44. (ii) The masthead's opening line — the sentence under its headline — becomes a theme setting: default = today's sentence, byte for byte; empty = no element for it | Nightwatch, in its own new file; axe unchanged; the template suite on a rig `OK (22 tests, 2643 assertions)`; the theme's job list 10/10; no tag | — |
| T-0722 ○ | T | **Two front-page texts that stop being true once the demonstration content is gone.** *Added 2026-09-25 by [ejecutor] under D-075, on the independent read-only audit's plan. Rests on necessity.* (i) With nothing published, the front page shows no "1 January 1970" date; with records, the date is unchanged — a functional test inside an existing class, in both phpunit jobs. (ii) The Fuentelclaro sentence moves out of `config/views.view.agora_base_publications.yml`'s `block_2` header into a Canvas `text` component placed LAST on the front page, exported from a rig and not hand-written, reworded to drop "below"; the theme floor is raised to `^1.2`, and the `recipe.yml` comment on the floor updated | After removing the demo, "Fuentelclaro" goes from 1 to 0 with one Canvas delete-and-publish; `config-inventory` and `ContentModelTest` counts predicted and matched; job list 10/10<br>**(i) landed in ad8bf86.** | — |
| T-0723 ○ | T | **The accessibility statement's description of this package's CI moves into `ACCESSIBILITY.md` — D-077's implementation.** *Added 2026-09-25 by [ejecutor] under D-075, on D-077.* By the independent read-only audit's count, 668 of the shipped statement's 1,737 words describe the template's CI; that text moves into `ACCESSIBILITY.md` | In place of the moved text, the statement keeps two sentences, a link to `ACCESSIBILITY.md` and a "To be completed" note for the owner's own review; `ACCESSIBILITY.md` gains the moved text; `packaged-claims`' one-copy rules still hold; job list 10/10 | — |

## Wave 3 · The launch — one event, in this order

Strictly sequential (`plan.md` §5). Theme first, so that every intermediate state is a coherent public
state.

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-0708 ○ | H | **Cut the theme's launch tag** — the version D-070 names — at a `1.x` commit whose job list is green, and read the **tag** pipeline | In `agora_theme`: `git ls-remote drupalcode refs/tags/<v>` resolves to that commit; its branch pipeline lists 10 jobs, all `success`, all `allow_failure: false`; the tag's line in the tags section of `bash tests/bin/watch-gate`, run from the theme's working copy, reads 10 jobs, all `success`, none permissive — **before** T-0709. A transient red (`error: 429`) is retried, never released beside<br>**WIDENED 2026-09-25, 0 rows, [ejecutor] under D-075:** blocked by T-1407 and T-0721; its tag pipeline's `nightwatch` must print the band guard's line | T-0707 · D-070 · unit 006 closed · T-0716 |
| T-0709 ○ 👤 | — | **[andres] creates the theme release** from T-0707's notes; if D-070 chose a new minor, he unchecks `1.2.` in the same sitting (D-050 part 2; a Wednesday) | Read by [ejecutor] afterwards: `release-history/agora_theme/current` lists `<v>` `published` with `covered="1"`; `<supported_branches>` holds exactly **one** branch; `packages.drupal.org`'s `agora_theme.json` lists `<v>`, and the time it appeared is recorded | T-0708 |
| T-0710 ○ | T | **The template's launch commit**: T-0706's text, and the theme constraint's floor raised **only if** T-0704(c) or the launched theme requires it; push and read the job list | Job list 10/10 — all `success`, all `allow_failure: false`; the `Drupal CMS` job's trace prints `Locking drupal/agora_theme (<v from T-0709>)` and `OK (1 test, 1 assertion)`, with the job id quoted; `phpunit` and `phpunit-pgsql` totals printed<br>**WIDENED 2026-09-25, 0 new rows, signed by [ejecutor] under standing delegation: the launch-day run of T-0704 had no row.** Plan §5 precondition 5 (*T-0704 measured on the day*) and §9's first risk (*T-0704 is re-run on the launch day*) are discharged here; T-0704's ✓ is the measurement of 2026-09-25. Before this commit is pushed, on a new rig guarded three ways: T-0704(a) at the Drupal CMS release current that day, (b) with the theme release T-0709 published, (c) with the lowest release the constraint admits — each `site:install` exit 0 and each suite's `OK (…)` line printed, every difference from CI's totals attributed to a class; `DrupalCmsCompatibilityTest` not run<br>**WIDENED AGAIN 2026-09-25, 0 new rows, signed by [ejecutor] under D-075 (D-068 ruling 5):** the two stops [andres] confirmed on 2026-09-25, re-measured on the launch-day rig at the released theme — `/contracts` 1280 × 800, Tab ×27 → 'Minor contract', ring clipped ≤ 0.1 px; `/people` 320 × 256, Tab ×20 → 'Annual remuneration', 0 px — each printed; a larger clip reopens T-0616's evidence<br>**WIDENED 2026-09-25, 0 rows, [ejecutor] under D-075:** raise the theme floor to `^1.2.3`, since the launched theme is required; re-run T-1406's widened checks at 1.2.3 | T-0706 · T-0709 |
| T-0711 ○ | T | **Cut the template's first stable tag** — the version D-070 names — at T-0710's commit, and read its tag pipeline | `git ls-remote drupalcode 'refs/tags/*' \| wc -l` prints `1`, resolving to T-0710's commit; the tag's line in `bash tests/bin/watch-gate`'s tags section reads 10 jobs, all `success`, none permissive — **before** T-0712 | T-0710 |
| T-0712 ○ 👤 | — | **[andres] creates the template's first release** from T-0707's notes, and replaces both project pages and the template's image from T-0707's drafts | The release feed stops answering *"No release history was found"*; both pages re-read through the D7 API: the template's body contains **0** of `in development` and **0** of `no stable release`; the template node's `field_project_images` has **≥ 1** entry | T-0711 |
| T-0713 ○ | T | **Post-release verification** — T-0704(d)'s checks again, now expected to pass | `release-history/agora_transparency/current`: the tag's version, `published`, `covered="1"`, `<supported_branches>` = the tag's branch; `packages.drupal.org/…/agora_transparency.json` **200**, listing it; the D7 API's `field_project_composer_types` = `["drupal-recipe"]`; on a new rig, `composer create-project drupal/cms` at the current release with its `minimum-stability: stable` untouched, then `composer require drupal/agora_transparency` **with no stability flag** printing `Locking drupal/agora_transparency (<version>)`, then `drush site:install ../recipes/agora_transparency` exit 0 — each read with its time | T-0712 |
| T-0714 ○ 👤 | T | **Closure**: the unit report, the budget accounting in the same commit, and a HOLD for [andres]'s Gate B | This file's own count printed; D-044's necessity test applied **row by row**; wave 4 recorded as **carried**, with an owner and a trigger — D-012 makes it non-blocking | T-0713 |

## Wave 4 · 007-bis — the marketplace application, after the launch and never blocking it

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-0715 ○ 👤 | — | **The marketplace application, by the channel D-073 names.** [ejecutor] prepares the answers and the listing material — description, labels, the five self-assessment items, the privacy, security and support attestations, a support link; [andres] asks, or submits | Every sentence in the draft points at a packaged file (`path:line`) or a job trace (job id), read the same day; the listing's accessibility value is **the attestation's own claim, never a stronger one** (T-0617); each of the five review criteria maps to exactly one packaged file; the date asked or submitted is recorded, and the reply when it comes. **If Ágora enters the installer's curated list:** its entry read back from `site-templates.yml` — package `drupal/agora_transparency`, the screenshot URL answering **200** with a 500 × 400 WebP, every link external — and D-072's usage question asked again about the SimplyTest.me demo that arrives with it | T-0713 · D-073 |
