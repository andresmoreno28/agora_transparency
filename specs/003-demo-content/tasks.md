# Unit 003 · tasks (append-only)

Budget: **34** tasks. Known rows **30**, reserve **4**, one per named risk that can add rows —
D-035's answer, D-010's answer, the GitHub-mirror prerequisite, and the accessibility-statement
ruling. Crossing **34** costs a rider signed by [andres] naming what it displaces (D-031).

⚠️ **Reserve accounting, 2026-08-27 — stated because spending a reserve silently is how a budget
stops being one.** T-1207, T-1208 and T-1209 take the count from **30 to 33**, and they are spent
on **none of the four named risks**: they answer **D-040**, which did not exist when this budget
was written. The front page shipped SQL that summed a text column on every database Drupal
supports, and no gate caught it.

**One reserve row remains, and two named risks are still open** — the GitHub-mirror prerequisite
(T-804, blocked on [andres] creating the repository) and the accessibility-statement ruling. If
both need a row, this unit crosses 34 and needs the D-031 rider. **That is a foreseeable overrun,
not a surprise, and it is written here while it is still cheap to plan for.** D-035 and D-010 were
both answered without consuming a row, which is why the reserve reached this point intact.

⚠️ **Reserve accounting, second entry, 2026-08-27 — the reserve is now SPENT, and the overrun the
paragraph above called foreseeable has arrived one row earlier than the arithmetic that predicted
it.** T-1210 (D-041) and T-1211 (D-042) take the count from **33 to 35**. The dispatch that ordered
them predicted **34 of 34**, the ceiling; that prediction is **one row short**, and the discrepancy
is recorded rather than reconciled away, because the rule three paragraphs down says the count is a
command with its output and never a number in prose. Run it: it prints **35**.

The arithmetic, term by term, so nobody has to re-derive it: 33 on disk before today · **+1**
T-1210, D-041 = A, the frame stated on the site and in `README.md` · **+1** T-1211, D-042 = A, the
generator `content/MEDIA-LICENCES.md` was already citing · **= 35**. D-042's own record says
*"Spending the last row here takes the unit to its ceiling"*, which is true of T-1211 taken alone;
what it does not account for is that D-041 = A costs **its own row**, and D-041's option A says so
in as many words — *"about 2 task rows"*.

**So the D-031 rider is no longer a contingency: this unit is at 35 against a budget of 34 and
needs one now**, naming what the thirty-fifth row displaces. And the two named risks are still
open on top of that — the GitHub-mirror prerequisite (T-804 → T-1202) and the
accessibility-statement ruling (T-1105) — so **each of those needs its own rider too** if it turns
into a row. Three riders is the worst case; one is due today. ⚠️ Nothing here is a signature.
[andres] signs riders; this paragraph only refuses to let the overrun be discovered at closure
(I-105).

The count is a command with its output, never a number in prose (D-031's method fix):

```
grep -cE '^\| T-(9[0-9]{2}|1[0-2][0-9]{2}) ' specs/003-demo-content/tasks.md
```

⚠️ **Ids go four-digit from wave 10**, as `specs/002-base-and-theme/plan.md:137` predicted. The
regex above is anchored and bounded because `T-[0-9][0-9][0-9]` matches the first three digits of
`T-1001`; verified not to match any unit-001 or unit-002 id.

---

> **Legend for the `Repo` column** — **`T`** = `agora_transparency`, the site template ·
> **`H`** = `agora_theme`, the theme (its own sibling checkout) · **`·`** = the process layer,
> which lives inside the template's repository.
> **Legend for the `#` column** — bare = pending · `✓` = done and evidenced · `⏸` = deferred with an
> owner and a named prerequisite · 👤 = requires [andres]'s hands.

## Wave 9 · The mechanisms are proven before a single node is authored

Nothing lands in `content/` in this wave. Every row below exists because a mechanism this unit
depends on **fails silently**, and a silent failure produces a green gate over an empty result.
Every probe must be **run**, not reasoned about — I-066, recorded today.

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-901 ✓ | T | ✅ **Signed 2026-08-26.** `~/agora-cms` rebuilt from zero — `ddev delete -Oy`, `rm -rf`, hand-written `composer.json`, 153 packages, `site:install minimal`, **10 of 10 upstream recipes applied, 0 failed**. `require` diff against `git show HEAD:composer.json` → **0 lines** (the harness sits in `require-dev` so the criterion is literally satisfiable; flagged as an interpretation). **No Composer path repository of any kind** — one repositories entry, `packages.drupal.org/8` — which is a stronger form of I-048 than the row asked for. `drupal/core-recipe-unpack` deliberately absent: left in, it rewrites `require`. Baseline denominators for T-1001: **92 enabled modules · 516 config objects**, snapshot `t901-baseline` retained. 🔴 **`composer show drupal/agora_theme` → `1.0.0`, from `ftp.drupal.org`, and the installed `templates/` holds 7 files with NO `views-view-table.html.twig`** (the local checkout holds 8; `git tag --contains dbbe934` → **0**). Every Views table on this rig renders through core's stock template. 🔴 **A hazard CLAUDE.md declares closed was open**: `~/agora-smoke/source` still carried a remote named `origin` with a **live push URL**, so a bare `git push` there would have reached the canonical repository. Renamed to `drupalcode`, push disabled, falsified — `git push` now exits **128**, `git fetch` still works. Also found: three unit-002 probe scripts from 2026-08-23 nobody claimed to have cleaned, one of which had run a full `site:install` — the concrete argument for *rebuild, never pull*. Original text follows. **The unit-003 export rig.** Rebuild the D-032 rig (`~/agora-cms`, or a successor named by `tests/bin/doctor`) so its `require` is exactly Ágora's dependency closure **including** `drupal/agora_theme`. Rebuild, never pull — a rig that was pulled is not a clean-install surface | `diff` of the rig's `require` against the template's `composer.json` `require` produces **0 lines**. `drush status` reports the site installed. **`composer show drupal/agora_theme` prints the resolved version and it is quoted in the row's evidence**; if it is `1.0.0` the row records, in writing, that `templates/views-view-table.html.twig` does not exist in it (I-063) and that every table rendered on this rig is therefore pre-fix. A rig built with a **path repository** to the local theme checkout is a **failed** criterion, not an acceptable substitute (I-048) | — |
| T-902 ✓ | T | ⚠️ **Its subject was cancelled and the row is NOT withdrawn (rule 8): D-035=C means no content will ship a `translations:` key, so this probe now stops a thing being built rather than enabling one — which is a legitimate outcome for a probe, and the cheaper of the two.** Its finding stays live in `IDIOMS.md` for whoever adds `translations:` later: the importer drops a translation for a language the site has not configured **silently, with no log line at all**. ✅ **Signed 2026-08-26, and it found a 🔴 that would have broken T-1001.** Both outcomes quoted on **fresh databases proven distinct by `system.site:uuid`** — six applies, six different UUIDs, so no run was a re-apply hiding behind `Existing::Skip`. Clean: `translation languages: [en, es]`, `hasTranslation(es): TRUE`. Dirty (only `language.entity.es` removed; the content file byte-identical by `diff -r`): `[en]`, `hasTranslation(es): FALSE`, **exit 0**, and the log query is the finding — **15 watchdog rows, all severity 6 'module installed', 0 matching `language`, `translation` or `langcode`, 0 at severity ≤ 4.** ⚠️ **Contrast with T-903, and it is the sharpest thing measured tonight: a missing binary logs a severity-4 warning; a dropped translation logs NOTHING.** The importer has a warning path and did not use one. 🔴 **And a recipe cannot ship `language.entity.es` in its own `config/` at all** — `ConfigInstaller::createConfiguration():409` reads `if ($this->isSyncing()) { continue; }`, so **config entities are skipped during a recipe's module install**; `und` never exists; `ConfigurableLanguage::postSave()` then dies with a fatal error in `updateLockedLanguageWeights()`. `config: import: {language: '*'}` does not fix it. **A working shape was verified end to end** and is carried into T-1001. Original text follows. **Probe: does a shipped ES translation survive a clean apply?** A throwaway probe — one node YAML carrying a `translations.es` block, plus the minimum config (`language`+`content_translation` in `install:`, `language.entity.es`, one `language.content_settings.node.*`). Applied on a clean rig. ⚠️ Nothing from this probe is committed to `content/`; it proves the mechanism, it does not ship | **Both** outcomes quoted. Clean: `drush php:eval` prints the node's translation languages and the list **contains `es`**. Dirty: with `language.entity.es` removed and the recipe re-applied on a **fresh** database, the same command prints a list **without `es`** — and the apply reports **no error and writes no log line**. The absence of the error is the finding, and it is what the row exists to demonstrate. ⚠️ A probe run against a re-applied site rather than a fresh one proves nothing: `RecipeRunner` imports with `Existing::Skip`, so the second apply imports zero entities and both runs would agree | T-901 |
| T-903 ✓ | T | ✅ **Signed 2026-08-26.** Export layout matches the precedent exactly: `content/file/<uuid>.yml` with the binary **beside it**, named `basename(uri)`. Clean: imported sha256 `006d4f15…` equals source sha256 `006d4f15…`, 897 bytes, both printed. Dirty: binary deleted, install **succeeds**, and **exactly one warning across 64 rows** — `type=recipe severity=4`, *"File entity %name was imported, but the associated file (@path) was not found."* That is the measured justification for `media-licence`'s cross-scope check: a template can ship a `file` entity with no binary, pass its own install, and hand the user a broken image. ⚠️ **A fresh database is NOT a fresh site** — `site:install` wipes the database and not `public://`, so the previous run's binary would have made the clean case pass on residue; the binary was deleted before each apply and its absence printed. **The `crop` question is answered:** crop entities are **not** dependencies of `media` and `--with-dependencies` never produces them; the published templates' `content/crop/` must come from a separate `content:export crop`; and they are **not required** — `focal_point` regenerated one automatically on a clean import from a recipe shipping none. ⚠️ Unverified and it is the whole point of shipping them: a regenerated crop is the **default centre**, a shipped one preserves an editor's focal point. ⚠️ The probe image was **synthetic**, not a real CC0 asset — named as a gap. Original text follows. **Probe: does a `file` entity plus its binary import from `content/`?** One CC0 image, one `file` YAML, one `media` YAML, exported with `drush content:export media --with-dependencies --dir=…` and re-imported on a clean rig | The exported layout matches the measured precedent — `content/file/<uuid>.yml` with the binary **beside it**, named `basename(uri)`. After a clean apply the file exists under `public://` with a **sha256 byte-identical** to the source, both hashes printed. The dirty case: delete the binary, re-apply, and confirm the install **still succeeds** while the log carries `File entity %name was imported, but the associated file … was not found` — proving the warning-not-error behaviour that `media-licence`'s cross-scope check exists to compensate for. **Also recorded: whether `crop` entities appear in the export**, since both published templates ship a `content/crop/` directory and nothing read so far explains what creates them | T-901 |
| T-904 ✓ | T | ✅ **Signed 2026-08-26, and the vacuous-scan problem was resolved differently from how it was briefed.** The brief proposed arming on *"a binary exists OR the manifest exists"*; the implementer refused it, correctly: an unarmed run would still have **no asserted denominator at all**, and would print the same words whether the scan worked or `find` silently returned nothing — I-028 again in a new word. The insight is that **"N binaries" is the SUBJECT of the scan, not its denominator**. What I-028 asks is *did the scan happen*, and that has an answer today: `content entries (working) = 3`, `content entries (packaged) = 1`, both **asserted, FATAL at zero, always load-bearing**. So the script proves the enumerator ran (it found the Canvas page) and the classifier ran (it sorted that page into non-media) before it says anything about media at all. **Three scopes, not two** — working, committed, packaged — because *tracked but not packaged* means an `export-ignore` is dropping shipped media, a real defect nothing else catches, while *working but not tracked* is merely uncommitted and is **counted and printed, never a finding**. Arming adds the manifest-rows half so a row whose file was deleted cannot read as "nothing to do here". **The word `clean` is reserved**: an unarmed run ends `0 findings - NOT YET LOAD-BEARING, there is no media in either scope`. **Five dirty cases watched failing** plus an armed-clean control. Original text follows. **`tests/bin/media-licence`**, to the contract in `plan.md` §6.1: two scopes, printed allow-list, `N > 0` asserted, six-column manifest at `content/MEDIA-LICENCES.md` | The runner prints `N binaries (working) · M (packaged) · K manifest rows · L allow-listed licences · F findings` with every number stated and `N > 0`. **All three dirty cases watched failing before the invariant is trusted** (the T-508 standard): unlisted binary → `1 findings` exit 1 · orphan manifest row → `1 findings` exit 1 · a licence off the allow-list → `1 findings` exit 1. Registered in `gate-a-wave3.sh`; the runner's total moves from **37** to a stated number | T-903 |
| T-905 ✓ | T | ✅ **Signed 2026-08-26. It needed no arming and that was stated rather than assumed** — its denominator is **3** today and non-vacuous, so `N > 0` stands exactly as written. Its header's **first paragraph** is its limit: *"THIS INVARIANT CANNOT PROVE THAT A PERSON IS FICTIONAL. Nothing can."* — plus two further limits up front: it reads person **entities**, not free prose, and it matches **full names, never surnames**, because a deny term on one of Spain's commonest surnames would fire on a legitimately fictional person and the only route back to green would be deleting a term. **7 shape patterns in `python3` because three carry check digits** (DNI/NIE mod-23, IBAN mod-97): a grep for *eight digits and a letter* fires on an ISO date followed by `T`; the validated form does not, and precision is what buys the right to be trusted. **A closed world over entity types**, so a new place a name can live cannot arrive unnoticed. **The roster is in its own scope** — otherwise a real name pasted into `PEOPLE.md` would vouch for itself; demonstrated as dirty case (g), which fires **twice**. ⚠️ **The remuneration control is a measurement, not a hope**: a person with `remuneration: 52000` and `severance: 18500` gives `0 findings` and `suppressed: 0`, so the figures never even reached the suppression path. **9 dirty cases watched failing.** ⚠️ Two deliberate deviations from the plan's §6.2: the email rule is **inverted** to *finding unless the host is an RFC 2606/6761 reserved name*, because DNS inside an invariant makes the verdict depend on the network and a real `.com` mailbox is the same leak as a real `.es` one; and an `ALLOWED_ORG_ADDRESSES` declaration exists printing **0**, added now because wave 10 will ship a town-hall address and whoever hits that red needs a sanctioned route that is not *delete the pattern*. ⚠️ **The 12-term deny list is a floor and says so**: T-1001 must add the real office-holders of whatever municipality the demo models. Original text follows. **`tests/bin/no-real-people`**, to the contract in `plan.md` §6.2 — **including its stated limit**: the script's own header says in the first paragraph that it cannot prove a person is fictional, and names the three things it does prove | Prints `N files · R roster rows · D deny-list terms · S shape patterns · F findings`, `N > 0`. `content/PEOPLE.md` exists and every row carries the word `fictional` and a provenance. Three dirty cases watched failing: a DNI-shaped string in a field · a person node absent from the roster · a `mailto:` at a `.gob.es` host. ⚠️ The remuneration figures must **not** be a finding, and a run over the real corpus proving they are not is part of the evidence — an invariant that flags the product is one somebody will weaken | T-903 |
| T-906 ✓ | T | ✅ **Signed 2026-08-26, and it needed no accommodation either — because its scope was widened.** The plan's §6.3 scoped the sweep to *"the same binaries `media-licence` scans"*, i.e. `content/`, which is **zero files today**. It is scoped instead to **`no-secrets`' own binary set** — every file its `is_text()` guard rejects, in both scopes — which is a strict superset, is what §6.3's own rationale actually names (*"the files `grep -I` skips"*), and has a denominator of **2** from the first run (`screenshot.webp`, working and packaged). Implemented **inside `no-secrets` as level 3**, not as a new script, per §6.3 and per T-1007's criterion. Markers: the APP1/EXIF marker (`Exif` and two NUL bytes), the XMP URI, `<x:xmpmeta`, `<?xpacket begin`, plus **RIFF/WebP `EXIF`/`XMP ` and PNG `eXIf` found by walking the chunk list**, not by substring — `EXIF` as four raw bytes turns up inside ordinary compressed data. **No new tool**: `grep` + `python3` stdlib, and `exiftool` is named in the header as the thing being refused, with T-803's reason. ⚠️ **The summary line says `binaries opened`, never `binaries scanned`** — `gate-a-wave3.sh` parses `scanned:[0-9]+` and takes the **last** match, so a second `scanned:` would have silently redirected G3's denominator from the text scan to the binary sweep. Dirty case run in situ against the real invariant: a built camera JPEG, an XMP JPEG and a WebP with a genuine `EXIF` chunk gave `6 binaries opened - 2 carrying EXIF - 1 carrying XMP` and **6 findings, exit 1**, each naming its marker and byte offset; a marker-free control and the real `screenshot.webp` gave nothing. Original text follows. **The binary-metadata sweep** (`plan.md` §6.3), extending `no-secrets` to the files its `is_text()` guard excludes **by design** — `grep -I` skips every image, so EXIF GPS and camera-owner fields are invisible to the secrets invariant today | Prints `N binaries opened · M carrying EXIF · K carrying XMP · F findings`, `N > 0`, `F == 0`. Dirty case: an unstripped camera JPEG → exit 1, **naming the marker and its byte offset**. **No new tool**: implemented with `grep`/`python3`, both certified on this host by `tests/bin/doctor`. Adding an external EXIF tool would move the toolchain floor (T-803) for one check and is a **failed** criterion | T-904 |

---

## Wave 10 · The corpus — two lanes, disjoint by repository

**Lane A (`T`)** touches only `config/`, `content/`, `recipe.yml`.
**Lane B (`H`)** touches only `agora-theme/`: `templates/`, `css/`, a new `config/optional/`,
`tests/src/Nightwatch/`. Zero shared files.

⚠️ **Lane A is internally SEQUENTIAL despite its disjoint files**, because every row passes through
**one export rig**, and a rig is a single mutable resource: two modelling sessions produce a
baseline diff that belongs to neither. Disjoint files are not disjoint work when the tool is shared.

⚠️ **The `tester` starts in parallel with both lanes and its early failures are expected.** An
assertion that the library view has 26 rows is *supposed* to be red until the 26th node exists. A
red wave-10 morning is the plan working, not a regression.

### Lane A · template

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-1001 ✓ | T | ✅ **Signed 2026-08-26, and the criterion was met by changing NOTHING.** Under D-035=C the multilingual half is gone, so the rise from 102 config objects is **zero** — stated rather than implied: `config-inventory` reports **102 config objects, 0 findings**, and `git diff --stat -- config/` and `-- recipe.yml` are both **empty**. The packaging property `RequirementsTest` guards holds by construction: **0** content files carry `_core`, **0** carry `default_config_hash`. ⚠️ **AMENDED AGAIN 2026-08-26, and this time the multilingual half is GONE. D-035 is signed C: demo content is English-only and rule 6 is amended.** So this row no longer adds `language` or `content_translation` to `install:`, no `language.entity.es`, no negotiation config, no six `language.content_settings.node.agora_base_*`, and no `translatable: true` sweep. **`config/` stays at 102 instead of rising to ~115.** What remains is the row's other half — whatever config the corpus genuinely needs, produced through the export rig, hand-transplanted into `recipe.yml`'s `base` area block. ⚠️ **A second correction, and it is about a criterion rather than scope:** this row's clause *"no `_core` key on any new file, **which is also what keeps D-033's four locks accurately described**"* rested on a premise D-035's research **falsified** — 103 of 104 recipe-installed config objects DO carry `_core.default_config_hash` on an installed site; `RequirementsTest` guards the packaged YAML and the locale system reads the database. **The packaging requirement stands and the clause after it does not.** The five real locks are in D-033's correction. Everything below is superseded and kept. ⚠️ **AMENDED 2026-08-26 by T-902's probe: the shape below is NOT the shape that works, and shipping `language.entity.es` in `config/` ends in a fatal error.** The verified shape is `config: import: language: [language.entity.und, language.entity.zxx, language.entity.en]` — **explicitly ordered, locked languages FIRST, because `'*'` fails** — plus `es` created by a **config action** (`language.entity.es: createIfNotExists`), because actions run after `installRecipeConfig` while anything in the recipe's own `config/` is created before the imports and therefore before `und` exists. Measured end to end on a fresh database; the failing variant was measured too. ⚠️ **Neither was measured on Ágora's real recipe** — the probe ran on `minimal`, and install order is exactly the variable that broke it, so T-1001 re-measures there. ⚠️ Also carried: `RecipeConfigInstaller.php:70` validates config entities, so a hand-written `node.type.*` with `help: ""` fails *"This value should not be blank"*. ⚠️ **And the likelier mistake was NOT probed**: translation present, language present, but the **bundle not translatable** — six bundles, a different code path, unmeasured. Original text follows. **Multilingual configuration**, produced **through the export rig** and never by hand (D-032=B — a translatability change is a modelling change): `language` + `content_translation` in `install:`, `language.entity.es`, negotiation, six `language.content_settings.node.agora_base_*`, and `translatable: true` on the prose fields. ⚠️ Both modules are **core**, so they add nothing to `composer.json` and need **no SBOM line** — the same reasoning already recorded in `recipe.yml` for `datetime_range`, `views` and `content_moderation`. **The `recipe.yml` edit is hand-transplanted into the `base` area block** (D-032 step 5); the tool never writes that file | `tests/bin/config-inventory` reports the new count and the rise from **102** is **stated, not implied**. `git diff --stat config/` names only files the baseline diff produced. `RequirementsTest` still green — in particular **no `_core` key** on any new file (`RequirementsTest.php:138`), which is also what keeps D-033's four locks accurately described. The `recipe.yml` diff shows only hand-made, area-blocked changes | T-902, **D-035** |
| T-1002 ✓ | T | ✅ **Signed 2026-08-26.** Terms per vocabulary, printed: `area` **7** · `document_type` **7** · `financial_year` **4** · `procedure_type` **5** · `status` **5** = **28**. Files carrying a `translations:` key — or the token at all — **0**, as D-035=C requires. ⚠️ **The three legal citations were met by NOT adding them, and the reasoning is the finding.** D-033 binds them to appear **once each**; they already do, in config unit 002 exported — each measured at exactly **1** across `config/`, `content/` and `recipe.yml`. Repeating them in term descriptions would have made it **twice each** and broken D-033. A criterion satisfied by restraint is still satisfied, and the reading is written down rather than buried. ⚠️ **AMENDED 2026-08-26 by D-035=C: the `translations.es` block per term is GONE, and so is the clean-install assertion that counted terms having an `es` translation.** English labels and descriptions per D-033 remain, and so do the three legal-citation exceptions (`convenio`, `subvención`, `importe de adjudicación`) appearing once each, in a description, as a citation. ⚠️ **Those three are now T-1011's subject**: they are Spanish passages inside English text, which is a live **SC 3.1.2** exposure whichever way D-035 went. Original text follows. **Taxonomy terms** for the five vocabularies — English labels and descriptions per **D-033**, with the three legal-citation exceptions (`convenio`, `subvención`, `importe de adjudicación`) appearing **once each, in a description, as a citation** — plus a `translations.es` block per term | Per-vocabulary term counts printed. `grep -L 'translations:' content/taxonomy_term/*.yml` returns **0 files**. ⚠️ **And a clean-install assertion, because the file being correct proves nothing** (T-902): after apply, a query prints the number of terms **having an `es` translation** and it equals the number of term files | T-1001 |
| T-1003 ✓ | T | ✅ **Signed 2026-08-26.** `views.view.agora_base_people` executed **rows_on_page=8, total_rows=8 — asserted BEFORE any markup question** (I-062). Then the rendered page: **1 table · 1 caption · 7 header cells carrying `scope="col"` · 8 data rows · 0 empty remuneration or severance cells**. `no-real-people`: **160 files scanned, 8 names found, 8 roster rows, all matched, 0 findings**; the one suppressed nine-digit value is a hex UUID fragment, so **T-905's remuneration control holds on the real corpus** — no figure reached the suppression path. 🟡 **2 of 56 cells ARE empty, and it is D-026's own warning made real:** the *Asset declaration* column for the Municipal Secretary and the Municipal Treasurer, who under art. 8.1.h) correctly have none because they are not elected. Legally right and ambiguous to a screen-reader user — **verbatim the argument D-026 used against the union type**. The remedy is an `empty` text setting on that one column in a unit-002 config file; escalated, not silently changed. Original text follows. **`Person` corpus and the org chart** — names, positions, `remuneration`, `severance`, `declaration`, area. Art. 6.1 requires *perfil y trayectoria*; art. 8.1.f) requires the two figures in a sortable table | `views.view.agora_base_people` reports **`rows >= 3` asserted BEFORE any markup assertion** (I-062). `tests/bin/no-real-people` exits 0 over the new files with its denominator printed. Every row in `content/PEOPLE.md` has a provenance. The rendered table carries `<caption>` and `<th scope="col">` on every header cell, and the remuneration cells are non-empty — **an empty `<td>` that is empty by design is indistinguishable from missing data to a screen-reader user** (D-026's own argument, now testable) | T-1002, T-905 |
| T-1004 ✓ | T | ✅ **Signed 2026-08-26. The library paginates, and it was counted in the rendered markup rather than in the config.** **28 Documents** (≥26 ✓). `/library` totals **33** rows: **page 1 = 25, page 2 = 8**; in the DOM, `/library` carries **26 table rows** (1 header + 25) and `/library?page=1` carries **9**. The pager is present with a *Pagination* heading and 4 links. The **3** exposed filters each return a non-empty result for one value and an **empty** one for another — search `budget`→9 / `aeronautical`→0 · area `Finance and Budget`→13 / `Municipal Police`→0 · year `2024`→18 / `2025`→0 — and ⚠️ **both empty values are legitimate members of their vocabulary**, not orphaned terms: Municipal Police is used by a contract, 2025 is the year the in-force agreements run in. A facet that can never be empty and one that is always empty are both untested facets. Original text follows. **`Document` corpus, at least 26 nodes**, so the document library **actually paginates**: `config/views.view.agora_base_library.yml` declares `pager: full`, `items_per_page: 25`. A 5-node demo never renders a pager, and the pager is where `heading-order`, focus visibility and target size are tested | Page 1 renders **25** rows and page 2 renders the remainder; both counts printed. The pager element is present in the DOM. The exposed filters (`grep -c "exposed: true"` → **3** today) each return a **non-empty** result for at least one value, and an **empty** result for at least one other — a facet that can never be empty and a facet that is always empty are both untested facets | T-1002 |
| T-1005 ✓ | T | ✅ **Signed 2026-08-26.** `contracts` **7** · `agreements` **4** · `grants` **4** rows, each asserted before markup. **4 distinct `procedure_type` values** (≥3 ✓) and **1 counterparty holding 2 contracts**. The art. 8.1.a) statistic, computed from the imported entities rather than from the authoring script: Open **2 / 326,350 EUR / 28.6% by number / 61.1% by volume** · Simplified open **2 / 146,220 / 28.6% / 27.4%** · Negotiated without publication **1 / 34,800 / 14.3% / 6.5%** · Minor **2 / 26,400 / 28.6% / 4.9%**. ⚠️ **Non-degenerate in both dimensions, and THAT is the criterion rather than the row count: the by-number and by-volume orderings DIFFER**, so the statistic carries information instead of restating how many rows there are. Original text follows. **`Contract` · `Agreement` · `Grant` corpora**, sized so the **art. 8.1.a) derived statistic is non-degenerate** — carried by name from `specs/002-base-and-theme/plan.md:52-57`, where it is recorded as a **legal requirement** and as unit 003's | Each of the three views reports `rows >= 3` before any markup assertion. **At least three distinct `procedure_type` values** and **at least one counterparty holding two contracts**; both counts printed by query. A single-procedure, all-distinct-counterparty corpus makes the statistic arithmetically true and informationally empty, which is the same failure as a green over an empty set | T-1002 |
| T-1006 ⏸ | T | ⚠️ **HALF SIGNED 2026-08-26, and the other half is BLOCKED on a ruling — it was not attempted rather than improvised.** Done: `datasets` **5** rows; **5 of 5** distributions resolve to a readable file at HTTP 200; every CSV parses with its rows and columns printed (budget execution 2024 **9×7**, 2023 **9×7**, contracts awarded **4×8**, grants awarded **2×5**, street tree inventory **12×6**). The budget execution table is the machine-readable Dataset D-026 calls the source of truth. 🔴 **NOT done: rendering the distribution as an accessible table.** The dataset's view display uses the `file_default` formatter, which renders a download link, and **no core formatter parses a CSV into a table** — it needs a Twig template (the theme) or a module (rule 2 / D-037). ⚠️ **The plan and the task list disagree about which repository owns it**: `plan.md` §3 lists it under *Theme repository*, this row and the carried-debt table say wave 10 lane A. **Needs one word from [andres], or a ruling under standing delegation.** Original text follows. **`Dataset` corpus and its distributions**, including the **budget execution table** — D-026: budget is not a node type; the machine-readable execution table **is** the Dataset, and that rendered table is the accessible source of truth | Each dataset's `field_agora_base_distribution` resolves to a real file that downloads. The CSV **parses**, and its row and column counts are printed. ⚠️ Also carried from unit 002's debt table: **rendering the distribution as an accessible `<table>`** — the rendered table's row count equals the CSV's row count, asserted, so a table that silently truncates is a finding | T-1002 |
| T-1007 ✓ | T | ✅ **Signed 2026-08-26, and the invariant is now ARMED.** `media-licence` reports **39 binaries (working) · 39 manifest rows · 5 allow-listed licences · 0 findings**, and the words `NOT YET LOAD-BEARING` no longer appear — I-079's arming rule fired on the first binary, with no edit and nothing remembered. All 39 are `own work`. Binary metadata swept by hand over all 39: **0 EXIF · 0 XMP**. PDF integrity: **34 of 34 structurally valid** — header present and the cross-reference pointer resolving to a table whose every offset lands on its matching object. **Weight: 201 files, 207,943 bytes — 203 KiB, against haven's 75 MB and byte's 8.8 MB; the ceiling is stated at 2 MB**, roughly ten times headroom. ⚠️ **No image ships, and that is a D-026 statement rather than an omission:** no bundle in D-026 has an image field, so adding one would be a finding about D-026, never a quiet addition. 🟡 **`no-secrets` opened only 2 of the 39** — `grep -I` calls a file binary only when it finds a NUL byte, and a PDF without an early NUL is classified as **text**, so the EXIF/XMP sweep never sees it. Owner [ejecutor], fixed in the same session. Original text follows. **Media** — images and document files, each with its manifest row and a nameable licence | `tests/bin/media-licence` reports `N binaries · N manifest rows · 0 unlicensed`, `N > 0`. `tests/bin/no-secrets`' binary sweep reports `0 EXIF · 0 XMP`. **`du -sh content` is quoted against a ceiling stated in this row** — measured context: `haven` ships **75M**, `byte` **8.8M**; a transparency template whose defining property is sobriety states its own number and defends it | T-904, T-906 |
| T-1008 ✓ | T | ⚠️ **CORRECTED 2026-08-26, HOURS after signing, and the correction is the more useful half.** The row shipped **8 `menu_link_content` entities** and its evidence said *8 links, 8 view paths, sets identical*. Rendered, the menu carried **16 links — every route twice**: the views already declare their own `menu:` page-display options, **nested**, with the titles T-603 chose, and the eight content entities duplicated them **flat, with no parent**. A keyboard user tabbed the whole navigation twice; a screen-reader user heard every destination announced twice with nothing to tell them apart. ⚠️ **The original criterion was satisfied and the product was defective**: *count of links == count of paths* is true of 8-and-8 and equally true of 16-and-8 if you count only one source. **Remedy: the eight content entities are deleted**; the views' own declarations already satisfy the row, nested rather than flat. Verified on a rebuilt install: **8 menu links, 0 blank cells on `/library`**. Found by rendering a page and counting, not by any gate. Original signature follows. ✅ **Signed 2026-08-26, and it CLOSED a gap rather than verifying one.** The brief said to verify T-603 first; `specs/002-base-and-theme/tasks.md:555` records that *nothing links to the six routes and no signed row owns menu placement*, so T-603 does **not** cover it. **8 primary-menu links · 8 declared view page paths · the sets are identical**, each resolving through Drupal's router to its view route. All 8 targets return **200** carrying the project's declared route marker — the caption — with `scope="col"` on every header cell (publications 4 · library 4 · documents 6 · datasets 8 · contracts 11 · agreements 8 · grants 7 · people 7). ⚠️ **Keyboard reachability could NOT be measured at authoring time** because the theme rendered no menu at all (I-086), and it is measurable now that the placements install: **16 menu links per page**, observed on the preview. Original text follows. **Navigation.** Unit 002's debt table records *"nothing links to the six per-bundle routes"* as owned by **T-603**, which is signed ✓. **Verify on a rendered page first**; close whatever gap remains with `menu_link_content` entities in `content/`, which are content and carry ES translations | The count of primary-menu links **equals** the count of the 8 view paths (`agreements` `contracts` `datasets` `documents` `grants` `library` `people` `publications`), both numbers printed. Every one is reachable **by keyboard** and each target returns 200 with a rendered marker. ⚠️ If T-603 already covers it, the row closes as **verified, with the evidence** — never as "done" without a rendered page behind it (I-059: a justification is checked for existing, never for being true) | T-1004, T-1005, T-1006 |

### Lane B · theme

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-1009 ✓ | H | ✅ **Signed 2026-08-26, and the recorded debt understated itself.** Unit 002 wrote that the fixtures *"supply their own `<h1>`, which papers over a genuine defect"*. They did — **and the fixture site had no `block` module, so `SimplePageVariant` supplied a SECOND one. Every scanned page carried TWO `<h1>` elements, nothing counted them, and axe does not flag it.** The assertion is `h1 === 1`, not `>= 1`: zero and two are both defects and `>= 1` hides one. Four placements in **`config/optional`**, and the directory was **falsified rather than preferred**: `ThemeInstaller:154` validates `config/install` **before** the theme is installed and every `block.block.*` depends on the `block` module, which a theme cannot require — measured on a block-less site, `config/optional` enables cleanly and the placements appear when `block` arrives, while the same file in `config/install` throws `UnmetDependenciesException` naming `block.block.agora_theme_page_title`. Core agrees: Olivero ships **13**, Claro **8**, all optional, neither any in install. Observed on pipeline `936572`, job `11775183`: **6 pages · 89-89 rules per page · 0 violations · 297 assertions across 11 tests**, up from 5 · 167 · 8. Config validated against Drupal's own schema — 4 objects, 0 failures — and falsified with a deliberately broken copy reporting 2 problems by name. Original text follows. **Default block placements** — carried debt, unit 002: *"The theme ships no default block placements — no page title, no `h1`, no menu on any real page; every axe fixture supplies its own `<h1>`, which papers over a genuine defect, and `menu`/`node`/`field.html.twig` are consequently never exercised."* Verified today: `agora-theme/` has **no `config/` directory at all** | The theme ships `config/optional/block.block.*` for page title, main content, primary menu and messages. **The axe fixtures stop supplying their own `<h1>`** and the run still reports **0 violations** with the page count stated. A Nightwatch assertion counts **exactly one `<h1>`** per scanned page — not "at least one": zero and two are both defects and `>= 1` hides one of them. `menu.html.twig`, `node.html.twig` and `field.html.twig` each render at least once, asserted by a marker the template emits | — |
| T-1010 ✓ | H | ✅ **Signed 2026-08-26.** The pager prints a literal `<h2>` and **ignores** `pagination_heading_level`, with the rule stated beside the code: a pager heads a region, so it is a sibling of the page's top-level headings; `heading-order` fires only on an increase of more than one, so `h2` is exactly **+1** after the `<h1>` T-1009 now guarantees and a **decrease** after anything deeper — the only level that is safe without knowing what precedes it. **Fixed in the template rather than a preprocess hook**, because core has already substituted its `h4` default by then, so a caller who chose `h4` and a caller who chose nothing arrive indistinguishable. **The prose fixture now sets no level at all; that absence is the test.** ⚠️ And the gate got stricter in the way the row demanded: `axeCounts` returns **which axe bucket** a rule landed in, every page asserts `heading-order` is in `passes`, and the summary compares `headingOrderRan` against the page count — **`heading-order reported on 6 of 6 pages`**, observed. A rule that did not run cannot have passed, and axe files an unapplied rule in a bucket that reads exactly like a pass (I-045). Original text follows. **Pager heading level** — carried debt: *"core defaults `#pagination_heading_level` to `h4` and `heading-order` fired during development. The template correctly prints what the caller sets — nothing makes the caller choose."* | The theme sets the level by a documented rule, and the rule is in a comment beside the code. A fixture page carrying a pager scans clean for `heading-order` **and the axe log names `heading-order` among the rules run** — a rule that did not run cannot have passed, and reporting it as passed is I-045's exact failure | — |
| T-1011 ✓ | T | ✅ **Signed 2026-08-26, and it MOVED REPOSITORY on evidence — the row is now `T`, not `H`.** The implementer sent to the theme **stopped without writing code**, and found something one rung earlier than the boundary argument it was given: ⚠️ **the three fragments never pass through this theme at all.** They render only on **admin routes**, and admin routes are rendered by **Gin** — measured: `system.theme` default `agora_theme`, admin `gin`, `node.settings use_admin_theme: true`, and on `/node/add`, `/node/add/agora_base_contract` and `/admin/structure/types` the gin assets count **1** and the agora_theme assets count **0**, while five front-end pages carry **0 fragments** between them. A `hook_preprocess` fires only when the theme is active; on those routes it is not, and that is true of **every** install because the recipe sets only `system.theme.default` while `drupal_cms_admin_ui` supplies Gin. **So the marking belongs where the sentence is authored**, and it is a mechanism about language rather than about phrases: `<span lang="es">` travels with the words, so a reword carries it instead of orphaning it, and no repository learns anything about another. **Three one-line edits** to the shipped descriptions. Verified end to end on a live install rather than inferred: `/node/add` **2** spans, `/node/add/agora_base_contract` **1**, `/admin/structure/types` **2**, rendering as `known in Spanish law as the <span lang="es">convenio</span>`. 🟡 **The axe half of the row could NOT be honoured and refusing it is the finding.** `valid-lang`'s scope is elements with `lang` **other than `<html>`**, and the theme emits **0** of them — on `/contracts` the 29 apparent hits are `hreflang` on taxonomy links and the true count of `lang=` is **1**, on `<html>`. So `valid-lang` would report **`inapplicable`**, and the gate at `axe.js:418` asserts `bucket === 'passes'`. Naming it would have forced that assertion to accept `inapplicable` — **the I-045 false green welded into the gate**, in the row that warns about it. It becomes honest the day the theme renders an element carrying `lang`, and these three never will, because Gin renders them. ⚠️ **`html-has-lang` is satisfiable and was left alone**: it already passes and adds nothing. Original text follows. ⚠️ **RE-POINTED 2026-08-26 by D-035=C, and it did NOT disappear — it shrank and turned around.** There is no Spanish page to mark English fragments on, because demo content is English-only. But there are **three Spanish fragments already shipped inside English config descriptions** — `importe de adjudicación` in `field.field.node.agora_base_contract.field_agora_base_amount.yml`, `subvención` in `node.type.agora_base_grant.yml`, `convenio` in `node.type.agora_base_agreement.yml` — rendering on English admin forms **today**. That is **SC 3.1.2 Language of Parts (AA)**, live now and owed under D-033 regardless of how D-035 was answered, and this was the only row that would have caught it. **New shape:** those three fragments carry `lang="es"`; the count of annotated fragments is printed; `html-has-lang` and `valid-lang` are **named in the axe rules-run list** and report 0 violations. ⚠️ axe checks that a `lang` value is *valid*, **not** that it is *correct* — so the count and a named spot-check are the evidence and the axe green is a floor. **No longer gated on D-035**; it is signed. Original text follows. **`lang` attributes on the English parts of a Spanish page** — **WCAG 2.2 SC 3.1.2 Language of Parts (AA)**. Config-sourced strings are English by D-033 and the Spanish translation cannot reach an installed site (D-033's own ⚠️, four locks). A Spanish page therefore carries English field labels, view titles, facet headings and pager text, and **unmarked they are a 3.1.2 failure**. Today no template in `agora-theme/templates/` emits a `lang` attribute anywhere. ⏸ **GATED ON D-035** — if [andres] answers B or C the shape of this row changes, so it is not started before the signature | Every English UI string on a Spanish demo page sits inside an element carrying `lang="en"`; the **count of annotated fragments is printed**. axe's `valid-lang` and `html-has-lang` are **named in the rules-run list** and report 0 violations. ⚠️ axe checks that a `lang` value is *valid*, **not** that it is *correct* — so the count and a named spot-check are the evidence, and the axe green is a floor, not the proof | **D-035** |

---

## Wave 11 · The pages, and the first thing worth looking at

**Sequential.** Depends on wave 10 green in both repositories.

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-1101 ✓ | T | ✅ **Signed 2026-08-26. The home page goes from 5 KB saying nothing to 18,003 bytes.** 6 indicators, 3 real labels, 2 tables, 2 captions, 11 body rows, and it **replaces the blank landing page in place** — same entity, same UUID, same `/home` alias — so `page.front` and `testFrontPageRoundTrip()` are untouched. ⚠️ **The sharpest criterion was met the hard way: every indicator is asserted against an INDEPENDENT `entityQuery`, never against the view that rendered it.** Read out of the served HTML, expected computed by `entityQuery('node')->condition('type',…)->condition('status',1)->count()`: Document 28=28 · Person 8=8 · Contract 7=7 · Dataset 5=5 · Grant 4=4 · Agreement 4=4 — **6 checked, 0 failures**. Nothing asks the view how many rows the view found. The search box is a real `<form action="/publications" method="get">` with **3 `<label for>` resolving to real control ids and ZERO placeholder attributes**, and it actually searches: `?search=budget` renders 9 rows against an independent title query of 9. 🟡 `/home` returns **301 to the site root** on a DDEV-served install — the branch `testFrontPageRoundTrip()` already knows, caused by `redirect`'s route normalizer; the row records **which surface it measured**, per T-605. Original text follows. **The home page** — a Canvas landing page in `content/canvas_page/` carrying the "what do you want to know?" search box and the key indicators. ⚠️ It **replaces** the blank landing page at `ff94a20d-…`, which is `page.front`; the alias `/home` rides inside the page's own `path` field and the `/home`-versus-`/page/1` round trip is already asserted by `ValidationTest::testFrontPageRoundTrip()` — **do not "fix" it** | `/home` responds as `ValidationTest` already pins (200 under `BrowserTestBase`; a DDEV-served install returns 301 to `/`, and the row records which surface it measured). The search box is a real `<form>` with a **programmatically associated** label, not a placeholder. **Every key indicator's number is asserted against an independent query, never against itself** — an indicator that renders whatever it counted is a number that cannot be wrong and therefore proves nothing | Wave 10 |
| T-1102 ✓ | T | ✅ **Signed 2026-08-26**, 26,466 bytes, alias `/institution`. Asserted **in the order the row makes the criterion**, which is the whole point: (1) `agora_base_people` rows **8**, `>= 3` — **before any markup question** — (2) `<caption>` present on 1 of 1 tables, (3) **7 header cells, 7 carrying `scope="col"`**, (4) the theme's scroll wrapper present with `tabindex="0"`. Cross-checked: rendered org-chart rows 8 == `entityQuery(agora_base_person, published)` 8. 🟡 **The separate offices table was WITHDRAWN, not forgotten**: it was built, measured working (7 rows, caption, `scope="col"`, scroll wrapper) and pulled because it needed a page-less view — see the row's finding. What ships carries Position, Area, Annual remuneration and Severance in one table, so all three nouns of *"org chart, offices, remuneration"* are present. 🟡 **`/institution` is reachable only by URL** — nothing links to it. T-1008 owns navigation and the dispatch forbade re-adding `menu_link_content`, so it is flagged rather than decided. Original text follows. **The institution page** — org chart, offices, remuneration in accessible tables | Assertions in this order, and the order is the criterion: `rows >= 3` → `<caption>` present → `<th scope>` on every header cell → the theme's scroll wrapper present. Reversing the order passes against a page with no table on it (I-062) | T-1101 |
| T-1103 | T | **Budgets and contracts** — the accessible table is the deliverable, and the **art. 8.1.a) derived statistic** is computed and rendered | The statistic is asserted against a count the test computes **independently**. Any chart is `aria-hidden="true"` with the table as its accessible equivalent, adjacent in the DOM. ⚠️ **Whether a chart exists at all depends on D-037**; if no module is signed, the row ships the table and says so — a chart is not implemented by improvisation, and no dependency enters without its `DECISIONS.md` line (rule 2) | T-1102, **D-037** |
| T-1104 ✓ | T | ✅ **Signed 2026-08-26 with its FIRST CRITERION AMENDED under standing delegation, because that criterion cannot be met by any site template on any host — and establishing that is worth more than the row was.** The original asked each distribution's response to carry a correct `Content-Type` **and** `Content-Disposition`. Four measurements settle it: a **PDF from the same field, same directory, same install returns `application/pdf`** while the CSVs return `application/octet-stream` — public files never reach PHP, so the header comes from the web server's own mime table (nginx: `grep -c csv /etc/nginx/mime.types` = **0** and `default_type application/octet-stream`; Apache reads a table whose line 2072 is `text/csv csv`). And **`Content-Disposition` is emitted by NO core path at all**: `File::getDownloadHeaders()` returns exactly `Content-Type`, `Content-Length` and `Cache-Control`, and `FileDownloadController` never calls `setContentDisposition()` — so even the fully Drupal-served private route fails it. Every escape breaks something already signed: `private://` needs `settings.php` edited by hand and *"installs clean, no manual steps"* is the product; a hook needs code and `RequirementsTest` requires **0 `*.info.yml`**; an nginx line configures a stranger's server. ⚠️ **AMENDED CRITERION, replacing the first only:** *downloadable* is defined **without reference to any header** — HTTP **200**, body bytes **==** the `file` entity's `filesize`, and body **sha256 == the sha256 of the file shipped in `content/file/`**. **5 of 5 on all three, 0 failures.** Plus what the template genuinely controls and does: `filemime: text/csv` on all five and `type="text/csv"` on every rendered anchor. **Met and unchanged:** 5 downloadable distributions == 5 dataset nodes, each with a record page at 200; and the link text names format and size — `Grants awarded 2024, CSV, 302 bytes` and four more — **as the anchor's own text**, so it is the link's accessible name. Before the change the accessible name was the bare filename and the size sat in a sibling `<span>` no screen reader reads as part of the link; `description_field: true` is the one key that moved it. 🟡 **Cosmetic residue, and it is the theme's:** core's `file-link.html.twig` still appends `<span>(302 bytes)</span>`, so the size reads twice. `#with_size` is set FALSE by exactly one core formatter and by no config key, so **nothing inside this repository can suppress it** — it needs a template override in `agora_theme`. Original text follows. 🟡 **HOLD, 2026-08-26 — two of the three criteria are met and measured; the third is not the template's to answer and is NOT edited here (rule 8).** **Met:** the count of downloadable distributions is **5** and the dataset count is **5** (`entityQuery('node')->condition('type','agora_base_dataset')`, published **5**), and *downloadable* is defined without reference to any header: HTTP **200**, body bytes == the `file` entity's recorded `filesize`, and body sha256 == the sha256 of the file shipped in `content/file/` — **5 of 5 on all three, 0 failures**. **Met:** every link text now names the format and the byte size (`Grants awarded 2024, CSV, 302 bytes` and its four siblings), delivered by `description_field: true` on the distribution field plus a description per item, so the *anchor* carries it rather than a sibling `<span>` no screen reader reads as part of the link. 🔴 **NOT met, and it cannot be met by a site template on any host:** the five CSVs are served `application/octet-stream` with **no** `Content-Disposition`, while a PDF from the same field, directory and install is served `application/pdf` — nothing in the template differs between those two responses, because public files never reach PHP and the header comes from the web server's mime table (nginx: **0** csv entries, `default_type application/octet-stream`; Apache in the same image: `TypesConfig /etc/mime.types`, line 2072 `text/csv csv`). And `Content-Disposition` is emitted by **no** core path, public or private: `File::getDownloadHeaders()` returns `Content-Type`, `Content-Length`, `Cache-Control` and nothing else. `private://` needs a hand-written `settings.php`; a `hook_file_download()` needs code and `RequirementsTest` requires 0 `*.info.yml`; an nginx line configures a stranger's server. **Needs [andres]: replace the first criterion or accept the finding** — see **I-093**. Original text follows. **Open data** — each distribution downloadable, each with a record page | Each distribution's response carries a correct `Content-Type` **and** `Content-Disposition`; the count of downloadable distributions is printed and equals the dataset count. The link text names the format and the byte size, so a screen-reader user learns what a link will do before following it | T-1103 |
| T-1105 | T | ✅ **RULED to unit 003 by [andres], 2026-08-26 — the row stays here and the budget stays at 34.** The statement's substance **is** this unit's axe denominators, and one written in unit 006 against numbers taken in unit 003 rots in the gap. ⚠️ `specs/002-base-and-theme/plan.md:94` still sends it to unit 006 and is **not edited** (rule 8); the ruling supersedes it. Original text follows. **The accessibility statement**, with a complaints channel. ⚠️ **DIVERGENCE, RULING NEEDED BEFORE THIS ROW RUNS:** `ROADMAP.md:101` puts it in unit 003; `specs/002-base-and-theme/plan.md:94` — later-dated and signed scope — puts it in **unit 006**. Planned here because its text is this unit's axe numbers and a statement written in 006 against numbers taken in 003 rots between them | Reachable at a stated path; linked from the footer of **every** demo page, count printed. It names the conformance level, the date, the evaluation method and the known gaps — including, by name, the **three WCAG 2.2 criteria axe does not check** (`2.4.7`, `2.5.8`, `1.4.10`; `plan.md` §8). **It contains no claim the gate does not measure**, and each claim in it cites the check that produced it. A statement asserting more than the gate proves is the marketplace's most legible failure | T-1104, **[andres]'s ruling** |
| T-1106 | T | **`screenshot.webp`** — replace the 6,686-byte placeholder. `specs/002-base-and-theme/plan.md:97` assigns this here by name. It must be *"the screenshot of the site, not the logo"* | The file's dimensions and byte size are printed and its **sha256 differs from the placeholder's**, both quoted. It shows a rendered demo page carrying real content — a screenshot of an empty view is a screenshot of no table at all (I-062, in image form). Byte size stated against the same weight discipline as T-1007 | T-1105 |
| T-1107 | 👤 T | **The visible preview.** Serve the site at `https://agora-smoke.ddev.site` from a **rebuilt** `~/agora-smoke` — rebuilt, never pulled — applying the template as an end user receives it. ⚠️ Neither WSL rig is ever edited or committed from, and both hold a full clone of this repository that is one mistake away from looking like a working copy. Paths from `tests/bin/doctor`, which beats any file | The URL serves **200**. Home, library, institution and one node page render, each with a rendered marker, and the four URLs are handed to [andres] explicitly. **The resolved `drupal/agora_theme` version is printed and it comes from `packages.drupal.org`** — a path repository to the local theme checkout demonstrates an installation the end user cannot perform (I-048) and makes the preview **inadmissible as evidence**. ⚠️ If the resolved version is `1.0.0` the row states, beside the URL, that the Views tables on screen are rendering **without** the fix at `dbbe934`, so nobody reviews a known defect as a design decision | T-1106, **`agora_theme` 1.0.1 published by [andres]** |

---

## Wave 12 · Gates, closure, verdict

**Sequential.**

| # | Repo | Task | Success criterion (falsifiable) | Blocked by |
|---|---|---|---|---|
| T-1201 | ? | **axe over the demo pages** — the point at which axe stops scanning fixtures. ⏸ **The surface is undecided and this row is GATED ON D-036.** D-027 established that Nightwatch **cannot be collected** in the template repository, and the demo pages live there. The row is written with its home unnamed on purpose; naming one here would be deciding D-036 by implementation | Whatever surface D-036 names: **`P` pages scanned with `P >= 6`**, the **rules-run count stated per page**, 0 violations, and the job **named in a job list read from the API** with its `allow_failure` value. ⚠️ **The per-page rules-run counts are compared to each other** — a page reporting fewer rules run than its neighbours is a page where something did not load, and it reads exactly like a pass. ⚠️ Each page's **denominator is asserted before its axe result** (`rows >= 3` / `h1 == 1`), because axe over an empty Views page reports no violations truthfully and about nothing (I-062) | Wave 11, **D-036** |
| T-1202 | ⏸ ? | **Playwright functional + visual regression** over the demo pages — **T-804 carried from unit 002**, where it was deferred on evidence rather than assumption (`gh repo view` → *"Could not resolve to a Repository"*). **Prerequisite: [andres] creates the GitHub mirror.** ⚠️ D-009(d) is binding: *"Visual regression is INFORMATIVE by definition: no task may reference it as a gate. Its workflow lives in the canonical repository and runs only on the mirror."* ⚠️ **Which repository holds it is part of D-036**: T-804 was written `H`, but demo pages exist only in the template — whose mirror already exists at `github/andresmoreno28/agora_transparency` | Baselines committed; the workflow reports **`S` screenshots compared** with `S` stated. **Informative, never a gate.** It may fail without blocking and **it may never lie** (D-020). ⚠️ **If the prerequisite is still unmet when wave 12 runs, this row is deferred a SECOND time in writing, with the prerequisite restated and the deferral dated** — a workflow committed to a repository GitHub has never heard of sits in the tree looking like coverage and reports nothing forever, which is the species D-020 was written about after nine consecutive green runs executed zero tests | Wave 11, **[andres] creates the mirror**, **D-036** |
| T-1203 | T | **Invariant sweep over the populated `content/`**, and the retirement of a check this unit legitimately breaks. **D-032 step 7's fourth pre-commit check is `find content -type f \| wc -l` still `1`** — true while `content/` held only the blank landing page, false from T-1101 onward. It is **replaced by name**, in the commit that first makes it false, by a check that asserts the count against the manifest instead | Every invariant prints its denominator (I-045): `no-secrets` (text **and** binary sweeps), `media-licence`, `no-real-people`, `no-code-in-template`, `no-boilerplate`, `sbom-check`. **`no-code-in-template` re-measures the packaged entry count** — it was **116** at T-703 and is stale by every content file and binary — and still reports **0 `*.info.yml`** in both scopes. `gate-a-wave1.sh` and `gate-a-wave3.sh` report their new totals; the movements from **61 · 0** and **37 · 0** are stated with the rows that caused them | T-1201 |
| T-1204 ✓ | · | ✅ **Signed 2026-08-26.** Site template: pipeline **`937268`**, ref `1.x`, commit **`d7cf665`**; theme: pipeline **`937289`**, ref `1.x`, commit **`813b32a`** — each read from `/pipelines/<id>/jobs`, each table naming its API path, **9 jobs · 9 success · 9 blocking** on both. ⚠️ **Both job LISTS are unchanged since the tables they replace, and they were re-read anyway**: a table nobody re-opened is a claim, not a measurement, and CLAUDE.md's own rule forbids a derived list. **Four denominators re-measured**, and two of them say something: PHPUnit **`OK (16 tests, 1951 assertions)`** — the **test count did not move and the assertions rose by 234**, which is the shape a unit that populated a corpus should produce, the same sixteen methods now walking 56 nodes and 8 rendered surfaces instead of a handful of fixtures; and the theme's axe line is **byte-identical** to the previous reading — `6 pages · 89-89 rules per page · 0 violations · heading-order on 6 of 6 · 297 assertions` — which is itself the finding, because the theme gained a template and its first `config/` directory in between and **the accessibility surface did not move**. Also refreshed: tracked-versus-spellchecked **386 / 347**, and `gate-a-wave3` at **43 checks · 13 invariants** where it read 37 · 11. Original text follows. **Refresh both gate tables from observation**, in the same commit that makes them true | Each table names its **pipeline id, ref, commit sha and the API path it was read from**. `jobs >= 9` on each; if the floor moved, the new floor is stated as a number in a paragraph, not edited into the quoted text of D-023(5) (rule 8). **No derived lists.** The four denominators CLAUDE.md carries — tracked-versus-spellchecked, PHPUnit tests and assertions, the install smoke, the invariant totals — are **re-measured, not carried forward**; the pair was 96 files stale once already | T-1203 |
| T-1205 | · | **`orquestador` audit and gate verdict for unit 003.** ⚠️ **Hard ordering constraint: unit 002's own audit (T-806) and closure (T-807) must have run first.** Starting unit 003 on an unaudited unit 002 is fine; **closing** it on one is not — the verdict would rest on a foundation nobody checked | No open 🔴. Every green quoted with its denominator. The **task count reported against 34** by the `plan.md` §4 command, with the command and its output both quoted. Every 🟡 carries an owner and a target unit. ⚠️ **The verdict states explicitly what the axe green does NOT cover** — the three WCAG 2.2 criteria axe cannot check (`plan.md` §8) — because a verdict that reports "axe clean" and stops has measured four of seven | T-1204, **T-806, T-807** |
| T-1206 | · | **Closure report and HOLD** for [andres]'s gate B signature | The report states, per repository: job list, test counts, assertion counts, pages scanned by axe **with rules-run per page**, files scanned by each invariant, `du -sh content`, and the resolved `agora_theme` version the evidence was produced against. **Any missing number is stated as missing, not omitted.** Gate B asks him for two judgements the gate cannot make: the **institutional tone** and the **plausible truthfulness** of the fictional content | T-1205 |
| T-1207 | T | **The front page's two aggregate blocks stop shipping SQL that sums a text column.** Done 2026-08-27, unsigned. D-040 = A: the three aggregated `sum` **fields** removed; the `sum` **sort** on `block_4` kept, because `GroupByNumeric::query()` never calls `addAdditionalFields()`. `block_3` keeps count, distinct areas and last-updated; its euro slot is **left empty rather than filled with a substitute**, per D-040's own warning that a plausible stand-in becomes permanent by inertia. `block_4`'s heading and caption rewritten to be true of a table with no money in it. Produced through the export rig (D-032=B), `uuid` and `_core` stripped, one trailing newline (I-092) | `grep -c "group_type: \(sum\|avg\|stddev_pop\)"` prints **1** — the sort. Generated SQL on a clean install: `block_4` emits **one `SUM()`**, on `field_agora_base_amount_value`, with no `langcode`/`bundle`/`delta` in any aggregate; `block_3` emits none. Clean install exit 0, front page 200. **Measured on PostgreSQL 16.15**, not read: the removed shape errors `function sum(character varying) does not exist`; the kept sort runs | D-040 |
| T-1208 | T | **`tests/bin/no-varchar-aggregate`** — the invariant that would have caught T-1207's defect. Fails when any display with `group_by: true` aggregates `sum`/`avg`/`stddev_pop` over a Field API table. Standard-library parser only: PyYAML is on the dev host and **not in the CI image**, and an invariant green because its parser could not load is the worst shape a gate can take | ⚠️ **Seen RED before green, and the red output is the deliverable**: 3 findings, exit 1, each naming display and line. After the fix 0 findings, same denominators, field entries inspected 10 → 7. The **safe** half falsified too — `count`, `count_distinct`, `min`, `max`, `group` on a field table not flagged; `sum` on a base table not flagged; `sum` **inherited** through an absent `defaults` key **caught**. Both refusal paths watched: unparseable file exits 1 naming the line, empty scope exits 1 rather than printing a comfortable zero. Parser cross-checked against PyYAML over **260 files, 0 mismatches**. Wired into `gate-a-wave3.sh`: **43 → 46 checks, 13 → 14 invariants**, CLAUDE.md moved in the same commit | T-1207 |
| T-1209 | T | **A PostgreSQL `phpunit` job in the drupalcode pipeline.** D-040(2): nine jobs were green over a query wrong on **every** database, and the assertions passed because nothing read that column. A job list is only as good as the environments it runs in, and this one ran MySQL and SQLite because those are the **defaults**, not because anyone chose them. Uses upstream's own `_TARGET_DB_TYPE: $POSTGRESQL`; **no upstream job redefined** — 42 upstream job keys checked, 0 collisions. Extends `.phpunit-base` rather than `phpunit`, because `no-blind-phpunit`'s pattern cannot distinguish an `extends:` reference from a command; that trap is now documented in that script's header and `CMD_PATTERN` is deliberately **unchanged** — a false positive costs an hour loudly, a false negative costs a release silently | ⚠️ **The job proves its own database rather than being trusted to have one.** The first success criterion written for this row — grep the log for `SIMPLETEST_DB=pgsql://` — was **useless and is recorded as such**: that line sits in a literal block GitLab echoes unexpanded into every phpunit-family trace, so the MySQL job already contains it and a `phpunit-pgsql` silently running MySQL would have passed. It now prints `_TARGET_DB_TYPE` and **exits 1** if it is not `pgsql`. Green when the API job list shows a **tenth** job, `status == "success"`, `allow_failure == false`, and its own trace carries `_TARGET_DB_TYPE=pgsql`. Floor moves `jobs >= 9` → `>= 10`; `watch-gate`'s declared name list and CLAUDE.md's observed table move with it | T-1207 |
| T-1210 | T | **The demo's frame is stated on the served site and in `README.md`.** Done 2026-08-27, unsigned. D-041 = A: the council stays `Fuentelclaro`, no corpus is rewritten, and what is added is legibility. One sentence now renders on the front page and one paragraph in `README.md`, both saying that the corpus is a **fictional Spanish municipality**, published in **English** per D-035, whose six registers derive from Spanish transparency law. ⚠️ **The sentence is NOT in the Canvas page, and that is a measurement rather than a preference.** `Drupal\canvas\Entity\Page::baseFieldDefinitions()` gives a Canvas page exactly one renderable body field — `components` — and the dispatch pins the `component_id` list; `title` is the page title and `description` is labelled **Meta description**, so it reaches `<head>` and no reader. The only remaining surface on that page is a view the page already places, so the sentence is a `header` area of type `text_custom` on `agora_base_publications` `block_2` — the block whose heading is *What this portal publishes*, which is the question the sentence answers. The `empty` area of the same view already uses that plugin, so nothing new is installed | `git diff` shows `uuid`, `path.alias` and the four `component_id` values of `content/canvas_page/ff94a20d-….yml` **unchanged** — the file is not touched at all. `tests/bin/config-inventory` reports **0 findings** and `objects carrying a byte above 0x7F: 2`, the count **unmoved**, because the shipped sentence is ASCII and writes `Agora` rather than the accented form (D-033). `tests/bin/spellcheck` is clean over a printed file count, with no new term in `.cspell-project-words.txt`. ⚠️ **Not covered by any assertion**, and it is named rather than implied: no test reads that sentence off a rendered page, so it can be deleted and every gate stays green. A `ValidationTest` marker for it is the honest follow-up | D-041 |
| T-1211 | T | **`tests/bin/generate-demo-media.py`** — the build script all 39 rows of `content/MEDIA-LICENCES.md` were already citing, which did not exist. Done 2026-08-27, unsigned. D-042 = A. Regenerates the 34 PDFs **derived** from `content/node/*.yml` and the media, file and taxonomy entities they reference — nothing typed — and writes the 5 CSV distributions, whose figures exist nowhere else in the package, from literal text. **Python standard library only**: PyYAML is on this host and not in the CI image, so the exported-YAML subset has its own reader, and there is no PDF library so the bytes are written directly. ⚠️ **Scope is 39 files, not the 34 the dispatch names**, because all 39 rows carried the false citation and fixing 34 of them would leave the same defect in the other five. The manifest's `source URL` column now names the script by path, and a new section says what is derived, what is literal, and that `/tests` is `export-ignore`d so the script is in the repository and not in the tarball | `python3 tests/bin/generate-demo-media.py` exits **0** reporting `identical: 39   differing: 0`: every shipped file reproduces **byte for byte**, so nothing in `content/` moved and no `filesize` or media `description` went stale. Falsified in both directions before it was trusted — one byte appended to a PDF gives `1 finding(s)` exit 1, `--write` restores it and `git status` goes clean; a stray file under `content/file/` is a finding too. The YAML reader's strictness was falsified against **seven** shapes and **three were accepted on the first pass** as bare strings, which is what the `_scalar()` guard closed. `tests/bin/media-licence` reports **39 binaries (working) · 39 (packaged) · 39 manifest rows · 0 findings**; `tests/bin/no-secrets` level 3 opens every binary and reports 0 findings. ⚠️ No invariant runs this script, so a future content edit can make the manifest's claim false again without any gate noticing — registering it in `gate-a-wave3.sh` is the honest follow-up and would take the runner past 46 | D-042 |

---

## Carried debt, by existing number

| Existing item | Carried as | State |
|---|---|---|
| T-804 / T-206(b) — Playwright visual regression | **T-1202** | owned, wave 12 · prerequisite **[andres] creates the GitHub mirror**; surface is part of D-036 |
| Rendering a Dataset's CSV distribution as an accessible `<table>` (002 debt table) | **T-1006** | owned, wave 10 lane A |
| Theme ships no default block placements; `menu`/`node`/`field.html.twig` never exercised (002 debt table) | **T-1009** | owned, wave 10 lane B |
| Pager heading level (`#pagination_heading_level` defaults to `h4`) (002 debt table) | **T-1010** | owned, wave 10 lane B |
| Art. 8.1.a) derived statistic (`specs/002-base-and-theme/plan.md:52-57`) | **T-1005** + **T-1103** | owned — model in wave 10, render in wave 11 |
| `screenshot.webp` placeholder (`specs/002-base-and-theme/plan.md:97`) | **T-1106** | owned, wave 11 |
| Menu placement to the six per-bundle routes (002 debt table, ruled to T-603) | **T-1008** | **verify first** — T-603 is signed; the row closes as verified with evidence, or closes the gap |
| D-032 step 7's `content == 1` pre-commit check | **T-1203** | owned, wave 12 · replaced by name, never deleted |
| D-010 (open since 2026-08-21, "postponed to unit 003") | — | **[andres] signs before wave 10 authors a node** |
