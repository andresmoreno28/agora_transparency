{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": [
    "Deliberately MINIMAL allowlist, of exact commands.",
    "Rule: never a trailing glob in a command that accepts output flags",
    "(-o, --output, --ext-diff, -c): that turns the permission into",
    "arbitrary file writing. See IDIOMS I-010.",
    "Do not allowlist directories whose content does not yet exist or has not been reviewed."
  ],
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git status --short)",
      "Bash(git branch --show-current)",
      "Bash(git log --oneline)",
      "Bash(git diff --stat)",
      "Bash(composer validate)",
      "Bash(composer validate --strict)",
      "Bash(ddev status)",
      "Bash(ddev drush status)"
    ]
  }
}
