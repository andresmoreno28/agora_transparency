# Ágora — project word list for cspell
#
# Consumed as cspell's `project-words` dictionary: this path is the default value of
# `_CSPELL_DICTIONARY` in the Drupal.org gitlab_templates cspell job, registered only if the
# file exists. There is no project `.cspell.json` — see D-024(2).
#
# ENTRIES ARE JUSTIFIED, NEVER BULK-IMPORTED. The job leaves an artifact named
# `_cspell_updated_project_words.txt` — "this dictionary plus everything that just failed".
# Copying it is one command to green and an automatic red: D-024(4) forbids it by name.
# Every line below survives the question "why is this a word this project legitimately uses?",
# and the answer is on the line or in its group header.
#
# NOT here, deliberately:
#   - British-English spellings (behaviour, licence, organisation, artefacts, normalise, ...).
#     `_CSPELL_EXTRA: '--locale en,en-GB'` declares the actual language of the text and
#     `dict-en-gb-mit` is bundled on the runner. Declaring a language is not silencing a check.
#   - The verbatim Spanish quotations of [andres] in DECISIONS.md. Declaring those would make
#     them permanently correct everywhere, forever; they are scoped in place instead, with
#     cspell:disable markers around each quoted block.
#   - Real misspellings. Those get fixed, not declared — D-024(3), fourth bucket.
#
# Mechanic — corrected, verified against pipeline 933311: cspell's dictionary parser sets
# `stripCaseAndAccents: true`, and its docs read as if that folds both case and accents onto a
# single entry. It does not, for accents: `fundacion` was already listed here and `fundación`
# was still reported as an unknown word in the same pipeline run. An unaccented entry does NOT
# cover its accented form; each accented form that needs to pass either gets its own entry or,
# where the dictionary is the wrong tool (see the D-024(3) bucket-4 row in DECISIONS.md), a
# scoped `cspell:disable`/`cspell:enable` pair instead. Whether case folding actually works
# (`agora` covering `AGORA`) was not exercised by this finding and stays an untested claim, not
# a verified one — do not assume it holds just because the accent claim turned out false.

# --- Proper nouns --------------------------------------------------------------------
# Our own identity, plus the third-party projects, vendors and tools cited as evidence in
# the decision record. A name is not a spelling error.
Ágora                 # the product
Andrés                # the human who signs the decisions
andresmoreno          # git author handle, in .mailmap and in shortlog evidence
drupalcode            # git.drupalcode.org, the canonical remote
gitaly                # GitLab's git RPC service, cited in D-022 on default-branch behaviour
caresphere            # existing Drupal site template, surveyed as prior art
provus                # existing Drupal site template, surveyed as prior art
ripplemaker           # Drupal.org vendor cited in the site-template survey
vaimo                 # Drupal.org vendor cited in the site-template survey
szeidler              # Drupal.org maintainer handle cited in the survey
drud                  # DDEV's original vendor namespace
jqlang                # jq's upstream org, appears in the toolchain PATH
winget                # Windows package manager, in the environment doctor output
msys                  # MSYS2, the Git Bash runtime tests/bin/ must survive on
ggrep                 # GNU grep under its coreutils-prefixed name
defusedxml            # Python XML library named in the sbom-check parser notes
bitstream             # font vendor named in the license inventory

# --- Technical vocabulary --------------------------------------------------------------
# Real terms of art in git, the shell, PHP tooling and Drupal, used correctly.
subshell              # covers subshells, subshell's
bytewise
symref                # git symbolic ref, central to the default-branch analysis
shortlog              # git shortlog
worktree              # git worktree
testdox               # PHPUnit --testdox
coreutils
findutils
localrepo             # Gitaly's internal/git/localrepo package, cited in D-022
etree                 # Python xml.etree, used by the sbom-check parser
charmap               # Python's charmap codec, named in the encoding preflight
webform               # the Drupal contrib project
akia                  # AWS access-key prefix, a detection pattern in tests/bin/no-secrets
baprs                 # the xox[baprs]- Slack token character class in no-secrets
pousr                 # the gh[pousr]_ GitHub token character class in no-secrets

# --- Identifiers from our own scripts --------------------------------------------------
# Shell and Python variable, heredoc and function names that appear in tests/bin/ and in the
# prose documenting those scripts. Renaming them to please a spell checker would be the tail
# wagging the dog.
termcnt               # gate-a-wave3.sh, deny-list term count
oldifs                # doctor, saved $IFS
denycheck             # no-boilerplate, heredoc delimiter
lockentries           # no-unstable-deps, heredoc delimiter
locknames             # sbom-check, heredoc delimiter
projectlist           # sbom-check, heredoc delimiter
pyeof                 # sbom-check, Python heredoc delimiter
netcheck              # sbom-check, preflight XML fixture
findtext              # Python ElementTree.findtext, called by the sbom-check parser
fromstring            # Python ElementTree.fromstring, same parser
getpreferredencoding  # Python locale.getpreferredencoding, in the encoding preflight
pythonutf             # from PYTHONUTF8=1, the encoding fix recorded in 001-foundation/tasks.md
decodeerror           # doctor, encoding-probe marker
othererror            # doctor, exception-class probe marker
msystem               # doctor, reports $MSYSTEM
infoyml               # gate-a-wave1.sh, counts *.info.yml files
ciallow               # gate-a-wave1.sh, CI_ALLOW_DEV check state
snone                 # no-boilerplate, printf format token %snone%s
smention              # no-ci-allow-dev, printf format token %smention
oneline               # git log --oneline
cand                  # doctor, $_cand candidate executable path
lver                  # no-unstable-deps, $_lver locked version
filt                  # no-blind-phpunit, CMD_FILT_RC / EXTRA_FILT_RC
fagora                # from the URL-encoded remote path project%2Fagora in the D-002 evidence
existe                # agora_no_existe_xyz, deliberately non-existent project name proving release-history returns HTTP 200 with an <error> body (I-013)

# --- English coinages we use deliberately ----------------------------------------------
# Regularly derived English words the bundled dictionary happens not to carry. Each is
# load-bearing in the decision record; none is a typo.
publishability
parallelizable
idempotently
bypassable
unparseable
unscannable
unsettleable
unfindable
automatable
overpromise
negotiables           # as in "the non-negotiables", CLAUDE.md's standing rules
desyncs
desyncing
retargeted
metacharacter

# --- Spanish that survives by decision --------------------------------------------------
# D-024(5) bounds the rename deliberately and records the remainder rather than half-fixing
# it. Every token below is a real, load-bearing identifier on disk that cannot be renamed
# without changing behaviour or breaking a signed record.
decisiones            # .claude/commands/decisiones.md — renaming it changes the /decisiones command
retomar               # .claude/commands/retomar.md — renaming it changes the /retomar command
orquestador           # .claude/agents/orquestador.md — the filename IS the subagent_type name
desarrollador         # .claude/agents/desarrollador.md — likewise a subagent_type name
ejecutor              # the [ejecutor] role label, pinned by CLAUDE.md non-negotiable rule 7
ciclo                 # .claude/skills/ciclo-agora/ — skill directory name
exportar              # .claude/skills/exportar-config-limpia/ — skill directory name
limpia                # .claude/skills/exportar-config-limpia/ — skill directory name
accesibilidad         # .claude/skills/accesibilidad-wcag-aa/ — skill directory name
licencias             # .claude/skills/sbom-y-licencias/ — skill directory name
fundacion             # the git branch 001-fundacion/scaffolding, recorded in D-022(4) and IDIOMS
# The three tokens below outlived the directories that put them here: the four unit directories
# were renamed to English in commit a0e01b6 (T-501), and every path on disk is English now.
#
# D-024's table prescribed "rename the paths to English - the token leaves the corpus; nothing is
# declared". For these three that outcome is unreachable, and the reason is structural rather than
# an oversight: DECISIONS.md (D-029) and the T-501 row in specs/002-base-and-theme/tasks.md both
# quote the pre-rename names permanently, because a record of what was renamed has to name it.
# Both files are spell-checked and cspell is blocking, so deleting these three lines turns the
# gate red. Amended, not edited (rule 8): the words stay, and this block says why, so nobody
# reads them later as evidence that the rename never happened.
#
# These comments deliberately do NOT spell the old directory names. T-501's success criterion
# greps the tracked files for them and excludes exactly two records; a justification that spelled
# them out would have made this file a third exception - the trap, in its third form, caught by
# running the criterion instead of trusting the edit.
tema                  # pre-rename Spanish name of unit 002's directory; quoted by D-029 and T-501
contenido             # same, unit 003
publicacion           # same, unit 007
estado                # specs/001-foundation/research/2026-08-20-estado-del-arte.md
arte                  # same research filename
flujo                 # specs/001-foundation/research/2026-08-21-flujo-tema-y-marketplace.md
encontró               # "no se encontró Python", quoted verbatim from the Microsoft Store stub in IDIOMS.md and gate-a-wave3.sh
proyecto               # survives only in D-024's own text, naming the pre-rename specs/000-proyecto/ path; rule 8 forbids editing it

dlx                  # pnpm's one-off package runner, used in the README's local spell-check pre-flight (T-225)

# Third-party identifiers reached once --dot put the hidden paths in scope (T-222).
enmod                # Apache's a2enmod, invoked in .tugboat/config.yml
tugboatqa            # Tugboat's own domain, docs.tugboatqa.com, cited in .tugboat/config.yml
webprofiler          # the Drupal module, named in the clean-export skill as one to uninstall

# ---------------------------------------------------------------------------
# Spanish transparency-law vocabulary. D-026 makes these the project's permanent domain terms:
# they name node types, fields and legal regimes, and every later unit will write them again.
transparencia    # the domain itself; also the project machine name agora_transparency
convenio         # Ley 19/2013 art. 8.1.b) - the Agreement node type
convenios        # plural, same article
Convenio         # sentence-initial and heading form
convenidas       # art. 8.1.b) 'obligaciones económicas convenidas' - a field name
obligaciones     # same phrase
económicas       # same phrase
subvención       # art. 8.1.c) - the Grant node type
subvenciones     # plural, same article; also the name of the BDNS register
adjudicación     # art. 8.1.a) 'importe de adjudicación' - a Contract field
adjudicatario    # art. 8.1.a) - the counterparty the derived statistic aggregates
licitación       # art. 8.1.a) 'importe de licitación' - a Contract field
licitadores      # art. 8.1.a) 'nº de licitadores' - a Contract field
procedimiento    # art. 8.1.a) - the procedure-type vocabulary
modificaciones   # art. 8.1.a) - a Contract field
importe          # the shared money field of the three financial regimes (T-601)
objeto           # the shared subject field of the same pattern
periodo          # the shared period field of the same pattern
contraparte      # the shared counterparty field of the same pattern
área             # the shared area/department field, and one third of the facet spine
beneficiario     # art. 8.1.c) - the Grant counterparty
partida          # the budget line D-026 rejects as a node type; the word survives in the reason
organigrama      # art. 6.1 - what the Person node type publishes
perfil           # art. 6.1 'perfil y trayectoria'
trayectoria      # same phrase
retribución      # art. 8.1.f) 'retribución anual' - a numeric Person field
indemnización    # art. 8.1.f) - the second numeric Person field
anual            # art. 8.1.f) 'retribución anual'
anuales          # art. 8.1.e) 'cuentas anuales' - a Document
cuentas          # same phrase
declaración      # art. 8.1.h) 'declaración de bienes' - a Person attachment
bienes           # same phrase
pleno            # 'acta de pleno', a document type in the vocabulary
plenos           # plural, as the autonomic regimes name it
actas            # same, plural of acta

# Institutions, statutes and registers named in D-026. Acronyms and proper nouns, not prose:
LCSP             # the Spanish public-procurement act - what a convenio is legally excluded from
BDNS             # the Spanish national grants register a subvencion must be declared to
Reglamento       # Reglamento (UE) 2023/138, which binds Spanish local entities directly
Reglamento's     # possessive of the same, used in T-614
Andalucía's      # possessive; the autonomic regime D-026 declines to chase
Cataluña's       # possessive, same sentence
Gobierto         # the incumbent non-Drupal transparency platform (Populate)
Terrassa         # a city running Gobierto, named as evidence of the incumbent

# Third-party identifiers reached by the unit 002 theme research:
Hyperlegible     # Atkinson Hyperlegible, an OFL typeface evaluated for T-607
hyperlegible     # lower-case form in the font's own package name
googlefonts      # the github.com/googlefonts organisation hosting the OFL sources
gstatic          # fonts.gstatic.com, the host the no-external-font grep forbids
lullabot         # Lullabot, cited as the source of a Drupal theming reference
uswds            # the U.S. Web Design System, compared against as a public-sector design system
opendata         # the drupal.org project of that name, measured and found unreleased
hostable         # self-hostable, in the licence discussion of T-607
Fdrupal          # a URL-encoded path fragment (%2Fdrupal) quoted from a drupalcode API call
zbdnk            # an opaque asset hash in a quoted Google Fonts URL


# The drupal.org project of the French State Design System, cited in D-033 as the precedent that
# settles the language question: it writes English config and ships French as a translation.
dsfr                  # drupal.org/project/dsfr, quoted in D-033
