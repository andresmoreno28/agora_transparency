# ported-from-theme.manifest - agora_transparency - the OTHER direction of D-028.
#
# WHAT THIS FILE IS, AND WHY IT IS NOT THE SIBLING'S MANIFEST WITH THE NAMES SWAPPED.
#
# `agora_theme` carries tests/bin/shared-invariants.manifest, which records the files it
# copied FROM this repository, plus a detector for local edits and, since D-063, one that
# reads this repository over the API. That machinery has a signed decision behind it
# (D-028), a named set of five invariants, and three layers of guard.
#
# Files travel the other way too, and until this file existed NOTHING in this repository
# knew it. tests/bin/executable-bit and tests/bin/preflight were both authored in the theme
# and ported here on 2026-09-06, in a commit whose own subject says so - "the template gets
# the two guardrails the theme has" - and that sentence in a commit message was the entire
# provenance record. A commit message is not a mechanism: it is read once, by the person
# who wrote it.
#
# ⚠️ AND THE HANDED LIST WAS ONE SHORT, WHICH IS THE MOST USEFUL THING ON THIS PAGE.
# Two files were reported as the ports. There are THREE. tests/bin/packaged-claims was
# written here on 2026-09-21 at 10:49 and shares 65 lines of 45 characters or more with the
# theme's tests/bin/claims-match-readme, which was committed at 09:44 the same morning -
# sixty-five minutes earlier. Only 2 of those 65 lines exist in this repository's own
# earlier tests/bin/claims-match-sources, so 63 of them arrived from the theme rather than
# being this project's house style flowing back.
#
# HOW THE THIRD ONE WAS FOUND, because the method is worth more than the file. Every script
# in both tests/bin/ directories was compared against every script in the other by counting
# shared literal lines of 45 characters or more - 38 files against 20, both directions, all
# pairs. That produces a BAND, and reading the band is the whole technique:
#
#   15-24 shared lines   the house style. The contract block, the house-rules paragraph,
#                        the I-028 sentence. Two files written here by the same hand score
#                        in this range (no-key-material against no-patches: 24).
#   37-65 shared lines   real shared substance: helper functions, argument parsers, whole
#                        printed paragraphs.
#
# Nine pairs scored above the band. Eight of them involve a theme file that ORIGINATED HERE
# - no-secrets, no-patches, gate-a-theme.sh and so on - so the shared substance is this
# repository's own, measured on its way back. Exactly one pair had a theme file that is
# genuinely the theme's AND older than its counterpart here: packaged-claims against
# claims-match-readme, 65.
#
# DIRECTION IS SETTLED BY TIMESTAMP, NEVER BY READING THE CODE. Two files that share 65
# lines tell you they are related; they do not tell you which way. `git log --reverse` on
# both paths does, and it is the only thing that does:
#
#   executable-bit       theme 2026-09-06T13:04:44   here 2026-09-06T15:14:49   (+2h10m)
#   preflight            theme 2026-09-06T12:42:31   here 2026-09-06T15:14:49   (+2h32m)
#   claims-match-readme  theme 2026-09-21T09:44:30   here 2026-09-21T10:49:01   (+1h05m)
#
# The same method run against the eight files the theme's own manifest records puts this
# repository first in every one of them, which is the control: a method that said "theme
# first" for everything would be measuring nothing.
#
# -----------------------------------------------------------------------------------------
# FIELDS, in order. THERE ARE SEVEN, AND THE SIBLING'S MANIFEST HAS SIX.
#
#   script         basename inside tests/bin/ HERE
#   role           invariant | preflight
#   status         verbatim | adapted
#   local_sha256   sha256 of the copy in THIS repository, as it stands now
#   source_path    the path in `agora_theme` the copy was taken from
#   source_sha256  sha256 of the blob at source_path at source_commit
#   source_commit  the `agora_theme` commit the copy was taken from
#
# ⚠️ `source_path` IS THE ADDED FIELD AND IT IS NOT TIDINESS. The theme's manifest derives
# the source path from the basename, because no file it copied was ever renamed. In this
# direction one was: packaged-claims came from claims-match-readme. A six-field manifest
# could not record that record at all - it would have to ask the API for
# tests/bin/packaged-claims in `agora_theme`, get an honest 404, and report broken
# provenance about a file whose provenance is fine. So the path is recorded rather than
# computed, and the two records where it happens to equal the basename are written out in
# full anyway: a field that is usually derivable is a field somebody derives.
#
# WHY EVERY RECORD IS `adapted`, and why that is the expected state here rather than a
# backlog. Measured against the recorded source, not estimated:
#
#   executable-bit    +64/-32     161 source lines -> 193 here
#   preflight         +275/-210   737 source lines -> 802 here
#   packaged-claims   +836/-437   656 source lines -> 1055 here
#
# These are not copies that drifted; they are copies that were REWRITTEN for a different
# subject on arrival. preflight runs the pipeline's lint jobs, and the two pipelines do not
# share a job list - the theme runs stylelint and nightwatch, this package runs phpstan over
# PHP tests and a Drupal CMS install smoke. packaged-claims went further still: the theme's
# guard refuses a figure stated twice IN ONE FILE, and this package's prose is spread over
# six files no reader opens together, so every registered figure here declares a HOME and
# the detector runs over all six. That is a different rule, not a port of the same one.
#
# The consequence for this manifest is that `verbatim` may never appear in it by accident.
# `adapted` asserts local_sha256 != source_sha256, and tests/bin/ported-copies fails a
# record whose status contradicts its own two hashes - which is the check that catches one
# hash being refreshed while the other is not.
#
# -----------------------------------------------------------------------------------------
# WHAT THE THREE LAYERS ANSWER, AND WHICH QUESTION EACH CANNOT.
#
#   this file                    what was taken, from where, at which commit
#   tests/bin/ported-copies      has a copy been edited HERE since        (offline, in the gate)
#   tests/bin/ported-drift       has the theme changed it SINCE          (over the API, in the gate)
#
# ⚠️ A CLEAN `ported-copies` RUN DOES NOT MEAN THE COPIES ARE CURRENT. It compares a local
# file against a recorded hash, so it answers "has this been edited here" - truthfully, and
# about a different question than the one that matters. That exact gap is what D-063 was
# signed over: 28 days and 125 genuinely-absent lines accumulated in the theme's
# identity-strings while its detector printed CLEAN, correctly. ported-drift is what asks
# the other question, and it runs on every push with nothing checked out but this tree.
#
# -----------------------------------------------------------------------------------------
# REGENERATING A RECORD, and step one is not optional.
#
#   1. `bash tests/bin/ported-drift` FIRST. It prints, per record, how far the SOURCE has
#      moved since the recorded commit. Read that number before deciding anything: a delta
#      is data, not an instruction, and the theme's own worked example is 132 upstream lines
#      of which 0 should have been taken.
#   2. Decide PER HUNK what belongs here. These three files were adapted on arrival; a hunk
#      that fixes a bug in shared logic belongs here, and a hunk about stylelint does not.
#   3. Re-derive BOTH hashes and read the commit with
#      `git -C <theme checkout> rev-parse HEAD`. Never type a sha, and never carry a hash
#      forward from the line above.
#      WHENEVER source_commit MOVES, DECLARED_COMMITS IN tests/bin/ported-copies MOVES WITH
#      IT, IN THE SAME COMMIT. Check 5 there compares the set of recorded commits against
#      that list, so a record moved alone fails with exactly one finding. And every answer
#      to a drift moves source_commit - taking the change, or staying behind on purpose -
#      so every answer needs that file too: a dispatch that names this manifest and not it
#      is a dispatch that cannot finish. (Added 2026-09-24, after these steps as written led
#      to exactly that hold.)
#   4. Re-run ported-drift and confirm the delta moved to exactly what you left behind.
#
# Neither detector has a `--fix` flag. A guard that can silence itself in one keystroke is
# the thing it was built to prevent.
#
# -----------------------------------------------------------------------------------------
# THE STATE ON THE DAY THIS FILE WAS WRITTEN, recorded because a first run that is green is
# a claim and this one is a measurement with a date on it.
#
# All three source blobs are byte-identical to `agora_theme`'s 1.x HEAD (bf433c9) right now.
# executable-bit and preflight have sat at a FIFTEEN-DAY-OLD commit, unwatched, and drifted
# by nothing. That is not a lucky result, it is the argument for how the cap is measured:
# tests/bin/ported-drift starts its clock at the first upstream commit that touched the path
# AFTER the recorded one - the moment a drift begins to exist - so these two report 0 days,
# not 15. A cap on the age of the RECORD would have opened this file with two findings
# against two records that are exactly right, which is the fastest way to teach the next
# reader that the check is noise.
#
# -----------------------------------------------------------------------------------------
# REGENERATED 2026-09-21 (T-0619), one record, and the four steps above were followed in order.
#
#   1. tests/bin/ported-drift FIRST: 3 records compared, 3 read, 3 provenance verified,
#      **0 behind** 1.x at a427375, 0 past the cap, 0 findings. So there was nothing upstream to
#      take and no per-hunk decision to make - the change is local adaptation only.
#   2. What changed HERE: packaged-claims gained LICENCE-MANIFEST.md as a seventh packaged
#      subject, four comparisons about it, one source-against-source comparison of
#      AccessibilityTest's DECLARED_PAGES constant, and a second --online block that reads this
#      project's own phpunit trace. None of it came from agora_theme.
#   3. local_sha256 re-derived from the file on disk. source_sha256 and source_commit are
#      UNCHANGED and were not retyped, because step 1 proved the source has not moved - a hash
#      carried forward from the line above is exactly what this manifest exists to catch.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0.
#
# The record stays `adapted`, which it must: ported-copies fails a record whose status
# contradicts its own two hashes, and these two were already 836 lines apart.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-21 (T-0629), the same one record, same four steps in order.
#
#   1. tests/bin/ported-drift FIRST, and it was run BEFORE the edit was decided rather than
#      to justify it: 3 records compared, 3 read, 3 provenance verified, **0 behind** 1.x,
#      oldest drift 0 days, 0 past the cap, 0 findings, network READ anonymously. So there
#      was nothing upstream to take and no per-hunk decision to make.
#   2. What changed HERE: ONE entry appended to packaged-claims' NOT CHECKED list - the two
#      figures content/MEDIA-LICENCES.md states about `haven`, which are facts about another
#      project at a version that will move and which no offline read can produce. Nothing
#      came from agora_theme and no executable line moved; the list goes 10 -> 11.
#   3. local_sha256 re-derived with `sha256sum` from the file on disk and substituted by
#      command, never typed. source_sha256 and source_commit UNCHANGED and not retyped,
#      because step 1 proved the source has not moved.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0.
#
# ⚠️ D-028 PERMITS THIS AND THE DISTINCTION IS WORTH RESTATING, because the wider version of
# it was invented once and cost six days. What D-028's 2026-08-24 amendment forbids is
# editing a copied invariant to make a red GO AWAY - weakening. It does not forbid editing
# one; option B's own cost line contemplates "one manifest to regenerate whenever a shared
# script legitimately changes", which is this. The edit here makes the guard say MORE about
# what it does not cover, which is the opposite direction from weakening, and the G11 checks
# assert that list is non-empty rather than asserting its length, so nothing was tuned to fit.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-21 (T-0614), the same one record, the same four steps in order.
#
#   1. tests/bin/ported-drift FIRST, and BEFORE the edit was decided rather than after it:
#      3 records compared, 3 read, 3 provenance verified, **0 behind** 1.x at a427375f,
#      oldest drift 0 day(s), 0 past the cap, 0 findings, network READ anonymously. So there
#      was nothing upstream to take and no per-hunk decision to make.
#   2. What changed HERE: packaged-claims gained ONE extraction (`admin scope`, which FATALs
#      if the corrected scoping sentence of the shipped accessibility statement is reworded
#      back to "which is Drupal core"), ONE comparison (`admin_dashboard_module`, which
#      derives the module name FROM the prose and looks for it in recipe.yml's install:
#      list) and ONE NOT CHECKED entry (Gin, Coffee and Navigation, which arrive from a
#      dependency's recipe and are named nowhere in this tree). Nothing came from
#      agora_theme; claims go 25 -> 27, comparisons 36 -> 37, the list 11 -> 12.
#   3. local_sha256 re-derived with sha256 from the file on disk and substituted by command,
#      never typed. source_sha256 and source_commit UNCHANGED and not retyped, because step 1
#      proved the source has not moved.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0.
#
# ⚠️ THE DIRECTION IS THE ONE D-028 PERMITS, and it is the same direction as the T-0629 edit
# above: the guard now says MORE about what it does not cover, and the G11 checks assert the
# NOT CHECKED list is non-empty rather than asserting its length, so nothing was tuned to fit
# and no check total moved - 95 / 66 / 24 is unchanged across this commit.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-23 (D-061 applied: the binding T-0615's closure held back), the
# same one record, the same four steps in order.
#
#   1. tests/bin/ported-drift FIRST, re-run on the day rather than carried from a run earlier
#      the same morning: 3 records compared, 3 read, 3 provenance verified, **0 behind** 1.x at
#      2134f7bd, oldest drift 0 day(s), 0 past the cap, 0 findings, network READ anonymously.
#      So there was nothing upstream to take and no per-hunk decision to make.
#   2. What changed HERE: packaged-claims gained FIVE extractions and SEVEN comparisons binding
#      what the shipped accessibility statement and README.md now say about the logged-in check
#      d47fd5a added - which page, read as which role, whose markup the contrast and landmark
#      findings are in, and that it is the only such page - to the constants AccessibilityTest
#      declares; ONE NOT CHECKED entry, for the stylesheet facts no offline read can reach; the
#      Gin / Coffee / Navigation entry and its twin comment reworded from "named nowhere in this
#      tree", which had stopped being true, to "declared nowhere", which is; and the three
#      recipe.yml line numbers in the T-0614 comment re-derived by grep. Nothing came from
#      agora_theme; claims go 27 -> 32, comparisons 37 -> 44, the list 12 -> 13.
#   3. local_sha256 re-derived with sha256sum from the file on disk and substituted by command,
#      never typed. source_sha256 and source_commit UNCHANGED and not retyped, because step 1
#      proved the source has not moved.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0; ported-copies 3 verified,
#      0 findings.
#
# ⚠️ THIS EDIT WAS HELD BEFORE IT LANDED, AND THE HOLD IS THIS FILE WORKING. The first dispatch
# permitted the script and not this manifest; with the script edited alone, wave 1 printed
# `95 checks - 2 failures`, both in G12. A copy edited here without its record moving is exactly
# what G12 exists to refuse, so the edit waited for the dispatch that permitted both. And the
# direction is again the one D-028 permits: every comparison added makes the guard say MORE.
# Wave 1 stays at 95 checks; the wave-3 figure is 67 since T-0632, not the 66 above.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-24 (T-0634, WCAG 2.5.8 switched on by name), the same one
# record, the same four steps in order.
#
#   1. tests/bin/ported-drift FIRST, and BEFORE the edit was decided: 3 records compared,
#      3 read, 3 provenance verified, **0 behind** 1.x at 2134f7bd, oldest drift 0 day(s),
#      0 past the cap, 0 findings, network READ anonymously. So there was nothing upstream
#      to take and no per-hunk decision to make.
#   2. What changed HERE: packaged-claims gained THREE extractions and FOUR comparisons
#      binding what the shipped accessibility statement and README.md now say about WCAG
#      2.5.8 - that the gate switches axe-core's `target-size` rule on by name - to
#      AccessibilityTest: the rule each document names against AXE_RULE_ENABLED_BY_NAME,
#      every window.axe.run( call against the calls that pass the options, and the
#      options builder against the one line that enables that constant. And ONE NOT
#      CHECKED entry, for the window size and the target counts the gate prints and the
#      prose deliberately does not quote. Nothing came from agora_theme; claims go
#      32 -> 35, comparisons 44 -> 48, the list 13 -> 14.
#   3. local_sha256 re-derived with sha256sum from the file on disk and substituted by
#      command, never typed: 4cfede61... -> 33f5fa04.... source_sha256 and source_commit
#      UNCHANGED and not retyped, because step 1 proved the source has not moved.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0; ported-copies 3
#      verified, 0 findings.
#
# ⚠️ WATCHED REFUSING THE EDIT BEFORE THE RECORD MOVED, which is the part of step 3 worth
# keeping: with the script edited and this record not yet regenerated, ported-copies
# printed `EDITED` for packaged-claims, `verified: 2`, `findings: 1`, exit 1. And the
# direction is again the one D-028 permits - every comparison added makes the guard say
# MORE, the G11 checks assert `> 0` rather than a length, and wave 1 stays at 95 checks.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-24 - THE FIRST TIME STEP 1 FOUND THE SOURCE HAD MOVED. The
# theme's 3e4e017 and a0f4cdf changed claims-match-readme, and this is the answer the
# 14-day cap asks for, given on the day the drift began. Same record, same four steps.
#
#   1. tests/bin/ported-drift FIRST: 3 records compared, 3 read, 3 provenance verified,
#      **1 behind** 1.x at a0f4cdf1 - packaged-claims, +260/-16 across 2 commits, oldest
#      drift 0 day(s), 0 past the cap, 0 findings, network READ anonymously.
#   2. Per hunk, read against claims-match-readme at a0f4cdf on drupalcode's anonymous raw
#      route, never a local checkout. Nine hunks, three taken in whole or in part:
#        H1 header, --online   PART TAKEN: its count-free "what --online checks" sentence.
#                              This copy's said "five figures" while --online has read
#                              SEVEN since T-0613. The per-section rule it adds: not taken.
#        H2 "Two entries"      TAKEN as "Some entries". This copy said "Three" and there
#                              are four since T-0634 - the same stale count, re-wrapped,
#                              so no line-level comparison could have seen it.
#        H3 --help text        not taken: this copy's --help is its own, and still true.
#        H4 README_FLOWED      not taken: every subject here has had a flowed variant since
#                              the first commit, which is the same capability, wider.
#        H5-H7 DOM, scroller   not taken: README's per-check assertion counts and the
#                              table scroller are the theme's subjects, and none is here.
#        H8 per-section parse  not taken, INCLUDING its ANSI and timestamp stripping, and
#                              that was measured rather than argued. This copy strips
#                              neither, so every trace it reads was fetched: nightwatch
#                              12413730, agora-invariants 12413729, phpunit 12412800. Each
#                              bound line matched once raw and once stripped, byte-identical
#                              3 of 3; the only escape codes on any of them wrap nightwatch's
#                              tick glyph BEFORE the match; every pattern is unanchored, so
#                              the timestamp prefix on every line cannot reach them. And the
#                              theme itself still greps its own axe summary line from the raw
#                              log at a0f4cdf - the stripping serves only the new parse.
#        H9 NOT CHECKED        PART TAKEN: "name the category rather than sample it", for
#                              the historical-figures entry. Two of the three examples this
#                              copy's entry named were in no packaged file any more.
#      The report handed over with those commits - none of the 63 lines that crossed on
#      2026-09-21 changed - was re-derived rather than accepted, by the header's method:
#      65 shared, 2 native, 63 ported, at 68 positions; all 63 verbatim at a0f4cdf; 0 of the
#      16 deleted source lines among them. True, and not sufficient: H2 changed a shared
#      SENTENCE.
#   3. source_commit read from the API's 1.x tip; source_sha256 from the blob at that commit
#      over the API, byte-identical to the web raw route; local_sha256 with sha256sum from
#      the file committed. All three substituted by command, never typed. ported-copies'
#      DECLARED_COMMITS moves with this record in the same commit - left behind, check 5
#      fails with exactly one finding, measured in a disposable copy before either moved.
#   4. ported-drift re-run afterwards: 3 / 3 / 3, **0 behind**, 0 past the cap, 0 findings;
#      ported-copies 3 verified, 0 findings.
#
# What else changed HERE, in the same commit because each moves this record: the NOT
# CHECKED entry for the haven and byte figures LICENCE-MANIFEST.md keeps (held on
# 2026-09-24 for want of this very file; the list goes 14 -> 15); "six files", which is
# seven since T-0619, and "eleven HTTP requests", a second copy of a bound count, both
# reworded to carry no count; and a sentence claiming the licence manifest states "four
# figures and no others", which the new entry would have contradicted. Nothing else moves:
# claims 35, comparisons 48 offline and 55 online, one-copy checks 8, wave 1 at 100.
#
# And step 3 of the procedure at the top of this file now says in its own words that
# DECLARED_COMMITS moves with source_commit. This regeneration needed a fifth file the
# four steps never mention, and the place to say so is the one the next person reads first.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-24 - preflight, THE FIRST TIME THIS RECORD'S SOURCE MOVED, and the
# change was about safety rather than lint: the theme's ff91c7c took the default rig away. Both
# copies fell back to ~/agora-smoke whenever no rig was named, and that directory is
# [andres]'s live preview. Same record, same four steps.
#
#   1. tests/bin/ported-drift FIRST, after ff91c7c was on drupalcode and before this copy moved:
#      3 records compared, 3 read, 3 provenance verified, **1 behind** 1.x at ff91c7c3 -
#      preflight, +181/-65 across 1 commit, oldest drift 0 day(s), 0 past the cap,
#      0 findings, network READ anonymously.
#   2. Per hunk, against the theme's own diff 82201a6..ff91c7c. Five hunks, ALL FIVE TAKEN, each
#      in this copy's terms - which is why this copy moved by +200/-81 where its source moved
#      by +181/-65:
#        H1 header       the no-default section, the quoted usage and the exit line. This
#                        copy keeps its own examples (--with-wave3) and states its OWN
#                        evidence: its lint-work-template/ left in the preview at 2026-09-06
#                        14:55, and its cache there written as late as 2026-09-24 00:47.
#        H2 arguments    which jobs need a rig is decided up front, and a missing rig stops
#                        the run before the banner - over this copy's four rig jobs.
#        H3 the rig      the block that reaches the container host moved ahead of the
#                        host-side jobs - THIS copy's block, byte for byte, not the theme's,
#                        whose comments differ - and the program that resolves the rig and
#                        refuses the preview, whose code is the theme's line for line and
#                        whose HOME note keeps this copy's wording.
#        H4 rig side     handed the resolved path, never the string as typed.
#        H5 rig script   no default and no tilde expansion left in it.
#      And three edits with no counterpart in the theme, because the sentences exist only
#      here: the shared-rig paragraph and the linted-copy comment both said "the same
#      default rig", and the bash -n note counted THREE shell programs where there are now
#      FOUR.
#   3. source_commit read with `git -C <theme checkout> rev-parse HEAD` at the moment of
#      copying, as this procedure says, and confirmed on drupalcode before it was written
#      here; source_sha256 from the blob at that commit over the API, byte-identical to the
#      theme checkout's; local_sha256 with sha256sum from the file committed. All three
#      substituted by command, never typed. DECLARED_COMMITS in ported-copies moves in the
#      same commit, and the set becomes THREE: executable-bit stays at 82201a6, because its
#      source has not moved and a record is dated by when it was read.
#   4. ported-drift re-run afterwards: 3 / 3 / 3, **0 behind**, 0 past the cap, 0 findings;
#      ported-copies 3 verified, 3 source commits on disk and 3 declared, 0 findings.
#
# ⚠️ WATCHED REFUSING IN BOTH DIRECTIONS BEFORE THE RECORD WAS ALLOWED TO MOVE. With the copy
# edited and this record not, ported-copies printed EDITED for preflight, `verified: 2`,
# `findings: 1`, exit 1. And with the record pointed at ff91c7c while drupalcode could not yet
# serve it, ported-copies was satisfied - 3 verified, 0 findings - and ported-drift was not:
# `preflight:source_commit` "answered 404 ... Provenance that only exists on one machine is
# not provenance.", 1 finding, exit 1. That is why this commit waited for the theme's push
# instead of landing beside it: an offline check that agrees with a record is not a
# provenance check.
#
# What else changed HERE: nothing outside the three files a regeneration of this record
# always touches. tests/bin/ported-copies' comment above DECLARED_COMMITS said two and says
# three, sitting over the three-line list it describes; its finding sentence and this
# file's footer stated the set's size as two and now state no size at all.
#
# -----------------------------------------------------------------------------------------
# REGENERATED AGAIN 2026-09-25 (T-0618, the security policy), the packaged-claims record,
# the same four steps in order.
#
#   1. tests/bin/ported-drift FIRST, before the edit was decided: 3 records compared, 3 read,
#      3 provenance verified, **0 behind** 1.x at ec2c990c, oldest drift 0 day(s), 0 past the
#      cap, 0 findings, network READ anonymously. The theme's 1.x has moved past a0f4cdf and
#      claims-match-readme did not move with it, so there was nothing upstream to take and no
#      per-hunk decision to make.
#   2. What changed HERE: packaged-claims gained SECURITY.md as an eighth packaged subject;
#      ONE extraction and ONE comparison binding its acknowledgement window to the D-064
#      heading that set it in specs/000-project/DECISIONS.md, read from the record and never
#      typed; ONE one-copy detector for that window, anchored on "calendar days"; and THREE
#      NOT CHECKED entries - the route, the coverage sentence and the sentence about recipe
#      unpacking, each a fact about Drupal.org or another project. Nothing came from
#      agora_theme; files 7 -> 8, claims 35 -> 36, comparisons 48 -> 49, one-copy checks
#      8 -> 9, the list 15 -> 18.
#   3. local_sha256 re-derived with sha256sum from the file on disk and substituted by
#      command, never typed: b40a2630... -> c2135dae.... source_sha256 and source_commit
#      UNCHANGED and not retyped, because step 1 proved the source has not moved, so
#      DECLARED_COMMITS in ported-copies does not move either.
#   4. ported-drift re-run afterwards: same 3 / 3 / 3 / 0 / 0 / 0; ported-copies 3
#      verified, 3 source commits on disk and 3 declared, 0 findings.
#
# ⚠️ WATCHED REFUSING THE EDIT BEFORE THE RECORD MOVED: with the script edited and this
# record not yet regenerated, ported-copies printed EDITED for packaged-claims,
# `verified: 2`, `findings: 1`, exit 1. The direction is the one D-028 permits - every
# comparison added makes the guard say MORE, and the G11 checks assert `> 0` rather than a
# length, so wave 1 stays at 100 checks.
#
# script            role       status    local_sha256                                                    source_path                     source_sha256                                                    source_commit
#
# -----------------------------------------------------------------------------------------
# executable-bit - ported 2026-09-06 in 434e289, role `invariant`, gate-a-wave1.sh G10.
#
# It reads the mode a shebang file is COMMITTED at out of the git index rather than from the
# filesystem, and that is the reason it was worth porting rather than reinventing: on this
# Windows checkout git does not track the executable bit, so `[ -x ]` answers TRUE for a
# file committed 100644 while the Linux runner answers FALSE. The theme paid for that
# lesson - its pipeline 950124 was red for a full push cycle behind a green local gate - and
# this repository got the finished guard instead of the same red.
#
# It was not vacuous on arrival: 2 of the 30 shebang files here were committed 100644.
#
# The +64/-32 is mostly the denominators and the prose around them. Both copies read the
# index; neither reads the filesystem.
executable-bit     invariant  adapted   9c1eeadc656e45af94dca93829d19c136ff3f0cf93a19ac3bacfac3ff0ccc011 tests/bin/executable-bit        e63e842a63211e3c125265e3550dfc5b593cf8dc9667e77cace0bc7ffd465373 82201a6a779f5f9bc6ee7bfe2b9782ef78af2dd4
#
# -----------------------------------------------------------------------------------------
# preflight - ported 2026-09-06 in 434e289, role `preflight`, in NEITHER gate runner.
#
# It runs the pipeline's own lint jobs against a rig with the pipeline's own fetched
# configuration, and it needs Docker, which is why it is not in the gate and why its role
# says so. The theme's measurements are the reason it exists at all: eight real phpcs errors
# sat undetected on a branch there, three real stylelint findings turned a pipeline red, and
# eslint had never been attempted at all.
#
# ⚠️ ITS ROLE BUYS IT NO EXEMPTION FROM THE CAP. A copy that is not in the gate is still a
# copy whose source can move, and this is the largest and most actively developed of the
# three, so it is the one most likely to move. Exempting a copy for its role would mean the record
# most likely to need answering is the one nothing asks about.
#
# ⚠️ THE SOURCE COMMIT IS NOT THE COMMIT THAT CREATED THE FILE THERE. preflight was born in
# the theme at 43505f2 (12:42) and changed again before the port at 82201a6 (13:04), which
# is the theme's HEAD at the moment of copying and therefore what is recorded. The blob at
# 43505f2 hashes to 5201a2cc..., which is NOT what was taken. Recording the birth commit
# because it is the one a reader would guess at would have produced a record that fails its
# own hash check.
#
# RE-SYNCED 2026-09-24 at ff91c7c, the theme commit that took the default rig away, so the
# record below names that commit and no longer 82201a6. The paragraph above is the account
# of the ORIGINAL port and stays as one; the regeneration note at the top of this file has
# the rest.
preflight          preflight  adapted   e1ce157c4c1a406140ca4f6b84ad26afb4344222250b921c70235506b0e6ace0 tests/bin/preflight             ee0df94a21ca98f70274eb16bf98907f5f38602b11f26d3b76654505a76adaff ff91c7c344cb964b4dab97bcb38e764e7234a065
#
# -----------------------------------------------------------------------------------------
# packaged-claims - written 2026-09-21 in d7cd919, role `invariant`, gate-a-wave1.sh G11.
# THE RENAMED ONE, and the record this manifest's seventh field exists for.
#
# Its own header cites the theme's guard and argues about it, which is what made the
# relationship look like a citation rather than a port. It is both: the argument is real and
# so are the 63 lines. What crossed is the machinery - the argument parser, `fatal`,
# `not_read`, the colour constants, the word-to-number table, the compare-and-print helper,
# the NOT CHECKED apparatus and several printed paragraphs verbatim. What did not cross is
# the rule: the theme refuses a figure stated twice in one file, and this one gives every
# registered figure a HOME across six packaged files.
#
# ⚠️ A CITATION IN A HEADER IS NOT A PROVENANCE RECORD, and this file is the whole reason to
# say so. packaged-claims names `agora_theme`'s guard nine times and a reader would call
# that well-documented. None of those nine mentions records WHICH COMMIT, so none of them
# can be checked, and no mechanism could ever tell you the source had moved. That is the
# difference between prose about provenance and provenance.
packaged-claims    invariant  adapted   c2135dae5c0a7456a7f3edcd5424eefdf7dc6a30bdf918a6f4b6016fdbbb3fbf tests/bin/claims-match-readme   8fe7bad3e5b89276a29009d1ab179acb2138ef6f68f0297684094be38d9a95b0 a0f4cdf13e3e72add7c9f6a09d8d3cf38b331e1b
#
# -----------------------------------------------------------------------------------------
# THE SOURCE COMMITS, DECLARED RATHER THAN COUNTED. tests/bin/ported-copies carries its own
# independent copy of the distinct source commits above and compares the two sets, so a
# half-finished re-sync - some records moved forward, some left behind, every individual
# record still telling the truth about itself - fails, because the SET stops matching the
# declaration. A manifest can be internally consistent and collectively wrong.
#
# WHAT NOTHING HERE CAN SEE, named rather than left to be discovered: a FOURTH file ported
# from the theme with no record in this file at all. The closed-world check below compares
# this manifest against a list declared in ported-copies, so a record deleted from here
# fails - but a port that was never recorded in either place is invisible to both. Finding
# the third one took a full cross-product of two tests/bin/ directories and a reading of
# both git histories; that is a task for a person, and it is written down here so the next
# person knows what it costs rather than assuming a green gate covered it.
